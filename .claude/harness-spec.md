# Harness Spec — Harness of Minervini v2

## Status and purpose

This is the maintainers' design record for the v2 Harness of Minervini. It explains where behavior lives, why the runtime is shaped this way, and how to change it without duplicating contracts. It is not runtime market doctrine and must not be loaded during an analysis session.

The approved implementation plan is `docs/plans/260817/harness-v2-greenfield-plan.md`. That plan is the decision-complete build record; this spec records the resulting architecture and maintenance invariants.

The product objective is an evidence-disciplined Minervini SEPA analyst for US-listed common stocks and ADRs that can assess the market, rank sector and industry leadership, discover candidates, analyze named tickers, and state conditional entry or active-position exit evidence. Portfolio allocation, position-size prescriptions, shorts, intraday trading, crypto, and non-US listings are outside scope.

The quality criterion is not imitation by prose volume. The harness must preserve Minervini's explicit gates and tacit decision standard while improving auditability, point-in-time honesty, missing-evidence handling, interface discoverability, and deterministic composition.

## Binding design decisions

- Principle over rail: `CLAUDE.md` supplies decision principles, invariants, precedence, and scope. Skills adapt those principles to user intent; they do not force one universal sequence or monolithic pipeline.
- Interface over document: exact syntax, defaults, inputs, limits, statuses, schemas, and side effects belong to the executable capability registry and each leaf command's offline `--help`, not duplicated prose catalogs.
- Dense information through progressive disclosure: the analyst loads the small constitution and one routed skill, then discovers only the capability contract needed for the next unresolved question through `capabilities`, `describe`, or leaf `--help`.
- Evidence over narrative: deterministic providers and reducers own precise market facts and verdict mechanics. Web research may explain current context but cannot fill numerical gaps, alter a hard gate, or masquerade as point-in-time evidence.
- Missing is not failure: known failed evidence and unavailable evidence remain separate throughout providers, reducers, envelopes, skills, and user-facing conclusions.
- No-trade is valid: the system is not required to manufacture candidates, bullish regimes, or BUY-READY outcomes.
- One shared harness: Claude Code uses `.claude/skills`; Codex reaches the same files through `.codex/skills -> ../.claude/skills`. `AGENTS.md -> CLAUDE.md` shares the constitution. No duplicated Codex copy exists.

## Information ownership

| Information | Authoritative owner | Duplication rule |
|---|---|---|
| Identity, scope, immutable analyst principles, data integrity, side-effect policy, routing | `CLAUDE.md` | Skills may operationalize but must not redefine these rules. |
| Triggering and adaptive task method | `.claude/skills/*/SKILL.md` | Keep only task-specific judgment and interface-discovery behavior. |
| Capability names, summaries, arguments, defaults, limitations, status semantics, side effects, examples | `scripts/minervini/capabilities.py` and `scripts/minervini/cli.py` | Generate or expose through `capabilities`, `describe`, schema, and detailed leaf `--help`; do not mirror the catalog in Markdown. |
| Executable doctrine claims, precedence, required inputs, failure and missing semantics, provenance | `doctrine/claims.json` through `scripts/minervini/doctrine.py` | Runtime skills use doctrine IDs and embodied principles; source attribution stays in the registry for audit rather than consuming analyst context. |
| Envelope and provider contracts | `scripts/minervini/contracts.py` and `scripts/minervini/providers/` | Schemas and tests must agree with code; prose summarizes only the invariant. |
| Detailed v2 decisions, migration order, and acceptance plan | `docs/plans/260817/harness-v2-greenfield-plan.md` | Do not convert the plan into runtime instructions. |
| Historical source corpora and prototypes | `.tmp/` | Build-time material only; ignored, edit-denied, and forbidden at runtime. |

The runtime skills deliberately do not cite books or maintain human-facing bibliographies. Their job is to make the analyst apply the normalized doctrine. Provenance remains machine-auditable in the doctrine registry, where it can support maintenance without bloating every analysis session.

## Runtime topology

The always-loaded root is `CLAUDE.md`; `AGENTS.md` is its symbolic link. The only routed runtime skills are `.claude/skills/market-scan/SKILL.md` and `.claude/skills/ticker-analysis/SKILL.md`; `.codex/skills` is a symbolic link to that directory.

There are no project agents, rules, workflows, or duplicate `.codex/skills`. Fixed scout agents and a `/screen` workflow were retired because concurrency and depth should follow the actual candidate set and unresolved evidence rather than a hard-coded fan-out rail. Cross-cutting JSON, schema, clock, retry, and side-effect guarantees are enforced by code and tests, not by advisory hooks.

One hook exists: a `SessionStart` readiness notice. It is not an analytical guarantee — those stay in code — but a report about whether the code can run at all, which no test inside the runtime can deliver to a session that is about to trust it. It runs only the offline half of `health`, so it costs no network, and prints nothing when everything is ready.

`.claude/settings.json` permits only the canonical pipeline and bootstrap command families and denies edits under `/.tmp/**`. Normal analysis remains read-only except for the ignored cache; chart artifacts and research-ledger mutations require their explicitly side-effecting capabilities.

## Interface architecture

The canonical entry point is `scripts/.venv/bin/python scripts/pipeline`. `scripts/pipeline/__main__.py` delegates to `scripts.minervini.cli`; the public analyst interface never calls legacy `scripts/modules` commands.

The registry exposes 18 composable capabilities: discovery and self-description (`capabilities`, `describe`, `health`, `clock`, `doctrine.show`), market work (`market.snapshot`, `market.candidates`), ticker work (`ticker.qualify`, `ticker.setup`, `ticker.fundamentals`, `ticker.peers`, `ticker.chart`, `ticker.risk`), and explicit research state (`watchlist.show`, `watchlist.history`, `watchlist.record`, `watchlist.annotate`, `watchlist.export`). The count and names are contractual and tested.

Every non-help invocation emits exactly one v2 JSON envelope with `schema_version`, `operation`, `request`, `as_of`, `status`, `data`, `signals`, `missing`, `sources`, `doctrine_ids`, `next_capabilities`, and `side_effects`. Status is one of `ok`, `partial`, `unavailable`, or `needs_input`; it describes contract completeness, not an investment recommendation.

`scripts/minervini/capabilities.py` is the metadata source for listing and description. `scripts/minervini/schema_sync.py` projects that registry into the 18 immutable-ID schemas under `schemas/v2/`. `scripts/minervini/cli.py` builds detailed offline help from the same meanings. Tests reject metadata, help, schema, and envelope drift.

Help is deliberately detailed at the point of use. Root and group help orient the caller; every leaf help explains purpose, required and optional inputs, defaults, as-of behavior, provider or historical limits, status meanings, side effects, and examples. Markdown teaches how to discover help but does not restate all flags.

Candidate pagination bounds both eligible rows and diagnostic bulk. Exclusion evidence retains the complete excluded-record count and complete counts by reason, while returning at most `min(limit, 20)` representative records; auditability therefore does not require sending the current security master's entire excluded population through model context.

`--format compact|full` changes detail only and cannot change verdicts, signals, missing-evidence meaning, doctrine IDs, or source truth. `--no-cache` bypasses cache reads and writes and exists for fresh diagnostics, not ordinary analysis.

## Doctrine and decision architecture

The normalized doctrine registry is the executable source of hard gates and precedence. It currently contains the standard Stage 2 and eight-of-eight Trend Template route, the bounded recent-IPO Primary Base route, VCP supply and setup claims, the narrow Power Play fundamentals exception, `[TL-EARLY]` confirmation debt and the five named early-entry tactics the practice layer defines, risk asymmetry and hard-stop claims, and quarantined non-executable material.

Precedence is scope, safety, and data integrity; Minervini eligibility and risk hard gates; verified explicit exceptions; tagged TraderLion practice-layer defaults; then current narrative context. TraderLion is integrated only where it fills a genuine execution gap without changing a Minervini gate. Conflicting tactics remain tagged and opt-in.

Pure evaluators are separated by decision concern: market regime and candidate scope, technical eligibility, setup and setup evidence, filed fundamentals, same-industry peers, and prospective or active-position risk. No weighted master score is allowed to let strength on one axis erase a hard failure on another.

The recommendation vocabulary is stateful and intentionally narrow. Qualification or `PROCEED` is not BUY-READY. Prospective outcomes are BUY-READY, WAIT, AVOID, or INCOMPLETE only after the relevant evidence converges; active-position outcomes are HOLD, SELL, or INCOMPLETE. Market and component operations retain their own descriptive states without pretending to make the final decision.

An active `HOLD` requires a current completed price and a clear completed-daily-low path from the stop's effective calendar date through the analysis session. Any historical breach produces `SELL` even if price later recovers; unavailable coverage produces `INCOMPLETE`. A raised or replaced stop is never projected backward before its supplied effective date, while an explicitly requested partial-session check remains a separate live-stop path.

## Data, identity, time, and cache

All analytical operations resolve one explicit point-in-time boundary through the New York market calendar. A default request uses the last completed US session. Explicit weekends and exchange holidays are rejected rather than silently shifted. Daily price evidence excludes an incomplete current bar, and provider requests use explicit end-exclusive boundaries.

Bar completion is decided at the provider boundary, never by each consumer. Yahoo can return a session with its price fields still blank; a row whose OHLCV values are not all finite is not a completed bar, `meta.as_of` is the last bar that survives that test rather than the requested boundary, and `meta.stale` marks the difference. A hole inside the history is typed unavailability instead of a silent drop, because a shortened window would quietly misreport every moving average. When price evidence stops before the requested session, the operations withhold the domain verdict rather than computing one from the earlier session and stamping it with this one; naming the earlier session with `--as-of` returns an aligned answer.

`meta.stale` means exactly one thing — this evidence did not reach the requested boundary — and it is also what stops a snapshot from being cached, since an unfinished bar will change. Temporal provenance that is normal rather than incomplete, such as reading a current-only page some hours after the close, is disclosed through `coverage` instead.

`ibd-rs-rating==0.5.0`, imported as `rs_rating`, is the sole authoritative first-party cross-sectional percentile source for this harness. The adapter calls its public APIs with an exact date, records package version and declared coverage, keeps `rs_raw` separate from `rs_rating`, and never reproduces the formula or calls the feed official proprietary IBD data.

Yahoo supplies price history and current classification through a narrow adapter. Current mutable sector and industry taxonomy cannot be projected into historical peer analysis. Nasdaq Trader's current security master supplies listing identity and eligibility scope but is likewise unavailable for historical reconstruction. SEC company facts and submissions are normalized only when `filed_at <= as_of`; period end alone is never sufficient, and requests are spaced under the published rate limit because SEC answers a burst by blocking the whole exit address.

Finviz publishes only a live page. It may stand in for the last completed session while no regular session is open, which is a question about the trading clock rather than the calendar date, and the envelope discloses how long after that session's close the page was read. Breadth built from it is context evidence, like the QQQ switch, not a measurement taken at the close.

Provider boundaries retry once, waiting between attempts, and then return typed unavailability carrying the underlying failure so a caller can tell a 403 from a certificate error without reproducing the call outside the CLI. That text is redacted before it leaves the boundary: query strings carry API keys and this harness's own SEC User-Agent carries the operator's email. Successful absence, withheld RS, stale coverage, malformed payloads, and transport failure retain distinct meanings. Every snapshot records source, retrieval time, effective as-of where supportable, version or coverage declarations, and a content hash.

Stdlib TLS is repaired on import when the interpreter has no usable CA bundle, because the python.org macOS builds ship without one and the RS client speaks stdlib `urllib` rather than `requests`. The repair sets `SSL_CERT_FILE` only when it is unset, and can never be the reason an import fails. `health` reports that bundle and the SEC User-Agent as local configuration without touching the network; `health --probe` additionally proves reachability, and unprobed health reports `reachability: not_checked` so readiness is never read as a claim about providers.

The provider cache lives at `.state/cache` by default and may be overridden by `MINERVINI_CACHE_DIR`. Keys include provider, operation, normalized parameters, and completed session. Writes are atomic JSON; corruption, schema mismatch, or expiry becomes a miss and rewrite. The cache supports JSON payloads, completed OHLCV frames, and stable security records without pickle. Research state is not part of this cache.

## Side effects and research state

Normal analytical capabilities do not mutate the research ledger. `watchlist.record`, `watchlist.annotate`, and `watchlist.export` are explicit user-authorized side effects and report what they changed in the envelope. Reads never create the database.

The default ledger is `.state/research-ledger.sqlite3`, overridable by `MINERVINI_LEDGER_PATH`. It stores stable instrument identity, as-of, output hash, verdict, conditions, invalidation, doctrine IDs, evidence quality, and notes. It is an auditable research memory, not an automatic portfolio manager.

`ticker.chart` is the other side-effecting analytical capability. It renders ignored weekly-first and daily PNG artifacts plus a manifest from the same completed-bar input used by the deterministic analysis. Visual judgment can resolve qualitative `needs_chart` evidence but cannot reverse a deterministic gate.

## Component inventory

| Component | Responsibility | Current state |
|---|---|---|
| `scripts/minervini/clock.py` | Completed-session, session-open, and explicit as-of resolution | Implemented and contract-tested. |
| `scripts/minervini/tls.py` | Stdlib CA-bundle repair for providers that bypass `requests` | Implemented; silent when the interpreter already has a bundle. |
| `.claude/hooks/provider-readiness.sh` | Session-start offline readiness notice | Implemented; silent unless the runtime cannot answer honestly. |
| `scripts/minervini/providers/` | Yahoo, RS, Nasdaq, SEC, and Finviz typed snapshots | Implemented with frozen provider fixtures and retry/PIT tests. |
| `scripts/minervini/cache.py` | Session-scoped atomic provider cache | Implemented and corruption/TTL/no-cache tested. |
| `scripts/minervini/technical.py`, `eligibility.py` | Trend Template evidence and eligibility routing | Implemented with standard and recent-IPO fixtures. |
| `scripts/minervini/setup_evidence.py`, `setup.py` | Deterministic observations plus explicit qualitative setup judgment | Implemented; absent chart judgment remains `needs_chart`. |
| `scripts/minervini/fundamentals.py` | Filed-as-of growth, integrity, leadership, and Power Play handling | Implemented with original/amendment cutoff fixtures. |
| `scripts/minervini/market_evidence.py`, `market.py` | Breadth, environmental context, group vectors, trade traction, and candidate scope | Implemented without a bullish weighted score. |
| `scripts/minervini/peer_collection.py`, `peers.py` | Stable-identity same-industry evidence | Implemented for current taxonomy with exact RS/date and completed-price checks. |
| `scripts/minervini/risk.py` | Final prospective and active-position reducers | Implemented with full completed stop-path, effective-date, recovered-breach, and missing-coverage tests. |
| `scripts/minervini/chart.py` | Auditable chart artifact generation | Implemented with input hash and manifest verification. |
| `scripts/minervini/ledger.py` | Explicit research-state persistence | Implemented with non-creating reads and export tests. |
| `scripts/minervini/operations.py` | Provider/evaluator composition and envelope data | Implemented with cache and operation integration tests. |
| `scripts/minervini/contracts.py`, `capabilities.py`, `cli.py`, `schema_sync.py` | Public interface, help, metadata, schemas, and output envelope | Implemented and parity-tested. |

## Verification strategy

All v2 tests live under `tests/260817`. The suite is layered into doctrine, unit, contract, integration, frozen provider fixtures, behavioral E2E, and v1 baseline evidence. Public seams were fixed before implementation and developed RED to GREEN under the repository's TDD contract.

Required deterministic gates are: doctrine registry validation; all reducer unit tests; provider, cache, ledger, chart, and operation integration tests; exact envelope and schema parity; detailed offline help coverage; harness topology; bootstrap; compile; dependency health; and the harness-creator validator.

Behavioral acceptance uses independent Codex runs over market, sector/industry, ticker qualification, setup, Power Play, same-industry comparison, active stop, missing evidence, point-in-time refusal, scope boundary, and side-effect prompts. Critical assertions require three independent passes, with an adversarial final synthesis checking for false BUY-READY, fabricated data, hidden portfolio sizing, and rail-driven overcalling.

The final v2 suite contains 167 passing tests. The first behavioral synthesis blocked release at 182/186 critical assertions because three active-position runs used only the latest close and one hypothetical recent-IPO run imported an unrelated fixture. Those failures were preserved in `tests/260817/e2e/round-1-findings.json`, fixed through public-seam TDD and closed-world skill guidance, and rerun by six fresh Codex agents. The final independent sol synthesis approved 186/186 critical and 86/90 noncritical assertions across 30 reports with zero release blockers.

Live smoke testing is limited to safe read-only provider and CLI paths. It verifies current integration but cannot replace frozen point-in-time contract tests. Network absence or source unavailability is reported honestly and is not treated as a deterministic failure of the doctrine engine.

The 2026-08-17 live report at `tests/260817/live/report.json` records healthy local dependencies, completed-session Yahoo prices, current market and security-master composition, representative large-cap, recent-IPO, ADR and excluded-instrument paths, active stop history, and honest Finviz/RS unavailability. It also records the candidate-response density regression and its reduction from a universe-wide exclusion dump to a 2,439-byte bounded summary.

The v1 diagnostic baseline is `tests/260817/baselines/v1/manifest.json`. The final v1 commit is preserved through the `harness-v1-final` annotated tag and GitHub release so obsolete runtime files can be deleted from v2 without losing recoverability.

## Design rationale

The v1 harness encoded substantial knowledge but spread the same facts among the root document, references, module documentation, a fixed agent, a workflow, and command implementations. That made drift likely and spent context before the analyst knew what evidence mattered. V2 keeps principles always available, task judgment in two skills, and exact mechanical detail behind a discoverable executable interface.

There is no trade-review skill in v2 because completed-trade grading is not part of the requested market, industry, sector, and live ticker analyst. There is no permanent scout agent because fan-out is an execution choice that should scale to the candidate set, provider health, and unresolved questions. The single `SessionStart` hook is deliberately the only one: deterministic contracts are stronger enforced by code and tests, and the one thing code inside the runtime cannot do is tell a starting session that the runtime itself is degraded. That gap is exactly what let the v2 rebuild run for a day without RS.

Legacy calculation code is not imported by v2. The old modules mutate import paths, install a process-global Yahoo proxy, mix network and stdout side effects with calculations, and frequently lack explicit historical cutoffs. Reusing isolated formulas would preserve those hidden couplings, so the v2 implementation promotes only independently tested concepts into package-clean modules.

The two host harnesses share files through symbolic links because Claude and Codex should receive identical scope, principles, and routed skills. A second generated copy would introduce drift without adding capability.

Obsolete v1 runtime and design documents are removed after the v1 tag and release exist. Git history is the archive; leaving live-looking duplicates in the worktree would make discovery ambiguous and invite accidental reuse.

## Maintenance protocol

When adding or changing a capability, first define or update the public test seam under the current dated suite directory in `tests/`, then change the registry, CLI parser/help, operation, schema projection, and contract tests together. A flag exists only when the implementation consumes it; decorative compatibility flags are prohibited.

When changing doctrine, edit the normalized registry and its doctrine tests before changing a reducer. Preserve provenance and precedence in the registry. Add text to `CLAUDE.md` only if it is an always-on invariant; add text to a skill only if it changes task-specific judgment; put syntax and defaults in the interface.

When adding a provider, declare present and historical support separately, use an injected or frozen raw fixture, retain source metadata and content hash, enforce the common as-of boundary, retry once, and represent unsupported reconstruction as typed unavailability. Never silently substitute one provider or formula for another.

When adding a side effect, mark it in capability metadata, disclose it in leaf help and the envelope, require explicit user intent, place generated state in ignored paths by default, and add a non-side-effect regression test for adjacent read operations.

Before accepting a harness change, run the focused RED/GREEN test, the complete `tests/` suite, help/schema parity, bootstrap and compile checks, and the harness validator. Update this spec in the same change whenever topology, information ownership, permissions, skills, or validation requirements change.

## Change history

- 2026-09-01 (v2.5 carried backlog 6+7): Closed the two items the previous round had recorded rather than fixed. Both were decisions rather than defects, and both turned on the same distinction once it was named: a week is a length when it bounds a thing being measured, and a date when it addresses a moment several names have to answer for at once.

  The registry listed `ticker.setup` as a consumer of three `basecount.*` claims and only one of the three was true. The two count claims require a `base_count` the capability has no field for and no flag to receive, and decision 309 had already given the count one home; those entries came out. The duration claim was the opposite error -- `setup_evidence` measured `base_duration_weeks` and read it against `setup.consolidation_footprint_3_to_60_weeks` alone, so the 5-to-26-week range the same corpus states for stage-2 base patterns had a consumer and was never emitted. The registry's own note on it says the two ranges "appear to describe overlapping but not identical things" and leaves a reducer author to decide which applies; nobody had, and the wider band answered by default. A 31.8-week base read as "within range" with nothing beside it, which is what the response standard forbids -- a band names which edge is the good one, and that base is on the wrong side of one of them. One measurement now reaches both standards. A reverse-direction guard was added and deliberately narrowed: `consumers` names which capability's analysis a claim is doctrine for rather than which reducer emits it -- 65 of the 169 claims name a capability and appear in no module -- so the check is only that a consumer registered for a measurement it must be handed can actually be handed it.

  The group new-high growth lookback stayed a bar offset after the 52-week window moved to the calendar. Both counts in that reading are over the same names, and each name was stepped back twenty of *its own* sessions: 28 days for one trading every session, 60 for one printing every third day. `earlier` was a sum over names observed at different moments, so a group read as growing because one constituent was measured further back than the others. It is not a thin-stock problem either -- a liquid name missing one exchange holiday steps back a day too far, and the independent review reproduced a `supports` that should have been `observed` across Thanksgiving. `convention.trading_week` converts a duration into a stock's own bars, which is right for a six-week flag and wrong for an address; its note claimed every week-stated limit passed through it, and that sentence was rewritten to what it actually owns. `build_market_evidence` now takes the reading date rather than deriving one, because a moment taken from whichever name had the freshest bar is a second clock beside the harness's own.

  Verification: 1,936 deterministic tests with the chunk sum compared against a whole-tree collection, 264 quotations verified and 6 declared, `validate_harness.py` clean, two adversarial review rounds by an independent model producing seven findings. Six were reproduced and fixed -- a band assertion that permitted the direction to reverse, a guard docstring promising more than the guard proves, `session_at_or_before` raising on a not-a-time where both readers promise to refuse, a reproduction resting on a thinly traded name alone, a reading date nothing pinned to the argument, and the contradicted trading-week note. One did not reproduce: twenty business days is exactly four weeks, so a `BDay(20)` implementation is the same date as 28 days for every weekday session and no test could distinguish them. Two items were recorded rather than fixed, each a decision: `setup_measurements` hardcodes five sessions to a week where the registry says that conversion is centralised, and the SEC host still answers 403 from this machine -- an edge block on the whole domain, confirmed against the bare root with no header, so Phase 7's fundamentals cases will use the recorded fixtures instead.

- 2026-09-01 (v2.5 Phase 6 carried backlog): Closed the three items Phase 6 carried, and each turned out to be one instance of the same shape: a rule that lived in one place and was enforced in another, or in none.

  `--base-count` was accepted on both `ticker.risk` modes and published on one. The registry scopes the `basecount.*` claims to `prospective_entry` and `setup`, and the reducer built the block only inside `management_evidence` -- a bundle keyed to a position being managed, which the prospective path never assembles and the active path empties on INCOMPLETE. So the mode the claim was written for validated the count and dropped it. An input taken and discarded is worse than one refused: the answer looks the same either way. Rather than add a second copy, the block moved: the count is a property of the advance rather than of a position, and both modes now publish it beside the risk controls. An adversarial round found three more, all reproduced -- a Python integer wider than a binary64 reached `float()` inside `_measurable` and escaped as `OverflowError` where the envelope is the contract (fixed at the measurement seam rather than by inventing a ceiling on bases the source never states), the new key was declared but not required so the contract still permitted the exact regression the slice had just fixed, and an active verdict cited both base-count claims outside their registered contexts. A guard now checks that every claim an envelope cites is registered for that request's own analysis.

  Quarantine was enforced by one sentence of the constitution. `doctrine._readable` refused a record marked `out_of_scope` (decision 12) and said nothing about a quarantined one, so "a quarantined claim never executes" had prose as its only home -- and prose is the layer that fails silently, because a reducer author reads the registry, finds a threshold, and nothing objects. The first cut refused the numbers, and an adversarial round walked through seven ways that was not enough: none of them read a threshold. A claim's binding authority, its required inputs, its `kind` and its stated rule all reach a reducer whole, and `setup_evidence._observation` published a withdrawn claim as somebody's binding standard on `binds()` alone; `power_play_evidence._registry_digest` serialised five claims entire into a key a chart reading depends on. So the boundary was redrawn as two accessors rather than as a ban on numbers: `get_claim` answers for anything the registry holds, because `doctrine.show` exists to show what was set aside, and `doctrine.claim` answers only for records this harness may act on. Everything a verdict passes through asks the second one. `validate()` gained the contradictions it had been accepting -- a claim named in the reducers' own threshold manifest and quarantined at the same time, a `status` of quarantine without the flag, a withdrawal with no reason recorded -- and `list()` now withholds `out_of_scope` as well, which had been publishing a position-sizing figure into the runtime listing since decision 12. The constitution sentence came out, and the `doctrine.show` declaration took over saying it.

  The 52-week window meant two things in two modules, which decision 307 had recorded and deliberately not fixed. `technical.py` bounded it by date; `market_evidence` counted `52 x convention.trading_week(5) = 260` sessions. Both directions were wrong: a real US year holds about 252 sessions, so 260 asks for thirteen months, and a name whose sessions were thinned by a halt reaches 260 bars over three years -- measured here at 1,087 days, publishing a high the stock printed 2.9 years earlier as its 52-week high. One reader now answers for both. An adversarial round found four, three of them regressions the move introduced: `pd.NaT` is an instance of `datetime` and `.date()` hands back another not-a-time, which raised out of the reducer; an index monotonic as text and not as dates was sliced positionally where a boolean mask had been order-independent, flipping criterion 7 from pass to fail; and rounding a timestamp down to its date made two moments 363 days and 17 hours apart read as a full year. All four were green because the first cut tested only what one module published and never opened the shared reader. The eligibility reading is unchanged, proven against sixty generated histories spanning 95 to 2,746 days.

  Verification: 1,915 deterministic tests, 264 quotations verified and 6 declared, `validate_harness.py` clean, three adversarial review rounds by an independent model producing fourteen findings, all reproduced and thirteen fixed. Three items were recorded rather than fixed, each because it is a decision rather than a defect: the registry lists `ticker.setup` as a consumer of the base-count claims and `ticker.setup` has no input that could read them; the group new-high growth lookback stays a session offset, which the registry's own convention states but which makes a group's "now versus four weeks earlier" count aggregate constituents observed on materially different dates; and SEC remains 403 on this machine, so the fundamentals plane has no live end-to-end confirmation.

- 2026-08-28 (v2.5 Phase 6 closed): Every item the phase named is done, and three the phase did not name were added on the way. What it set out to do was composition provenance, an integrity check on cited doctrine ids, a CIK lookup, and one pass over prose that had been written twice. What it also found was that `data` was the one envelope field with no contract at all, that four of the nine price consumers normalised their own frames, and -- from that audit -- that "52-week high" meant two different things in two modules.

  The pattern across the phase is one shape found in new places: a rule stated where nothing enforces it. A word the caller typed standing in for a measurement; a schema pinning eleven fields and leaving the twelfth open; a sentence in a skill body restating the constitution it was loaded beside; a generator writing all but one of its own artifacts. Each was closed by moving the rule to a surface that is read whether or not anyone remembers it -- an envelope reference, a baked vocabulary, a shared reader, a check with its own failure message.

  Suite 1,864 across nine dated directories, and the chunk sum was compared against a whole-tree collection because a suite run in chunks can pass entirely while a directory silently never runs. `validate_harness.py` clean, no spec drift, 264 of 270 quotations verified with six declared departures.

  Carried forward: `--base-count` is validated and discarded on the prospective path; `doctrine._readable` refuses `out_of_scope` structurally but leaves quarantine to a sentence; `market_evidence` still counts its 52-week window in sessions where the eligibility gate now bounds it by date. And the SEC host answers 403 from this machine, so the filings path and the new lookup are unverified end to end -- an environment condition, not a code one.

- 2026-08-28 (v2.5 Phase 6-F): Gave a required value somewhere to come from. `ticker.fundamentals` against a past session returns `needs_input` with `missing: [{"id": "cik", "reason": "stable_historical_identity_required"}]`, and no capability in this harness produced a CIK -- the value is required, the interface did not carry it, and `next_capabilities` was empty beside the gap, so the envelope named what was missing and left the analyst to find it somewhere the harness cannot vouch for.

  The lookup was always there, one call inside another. `providers.sec.fetch_company_tickers` is what the fundamentals path calls when no `--cik` is given and what the health probe uses; what did not exist was a surface a person can run. `ticker.cik` is that surface, and the gap envelope now names it.

  Why it refuses a past session is why `--cik` is a caller's assertion at all. `company_tickers.json` is a mutable current snapshot and a symbol can be reassigned, so today's mapping is not evidence about who filed under it a year ago; answering a historical request from it would be current security-master data relabelled as historical, which the constitution refuses outright. It answers for the current session and says what the answer is. A symbol the list does not carry is a different gap from a list that could not be read: the source answered, and no registrant files under that symbol.

  The `data` vocabulary contract caught the new capability on its own. `set(REQUESTS) == set(CAPABILITIES)` fails the moment a capability is declared without being swept, which is what that contract was for -- a new capability cannot enter unvalidated. Three more enumerations did the same for the CLI paths, the expected-capability set, and the published schema catalog.

  Two pieces of the capability's own honesty leaked back out and were closed. The pointer at this capability lands on its refusal when the same request is carried forward, so the refusal now names the step between them rather than leaving it to be inferred; and the map it reads was cached without a lifetime, which held a fact still for a whole session while saying that being current is what makes it unusable for a past one. It carries the same fifteen minutes every other current-only snapshot here does.

  That last one found a generator writing all but one of its own artifacts. `synchronize()` wrote a schema per capability and never touched `catalog.json`, so the index beside twenty generated files was hand-maintained and went stale the first time a capability was added. Running the generator then looked like bringing the directory up to date, which is worse than a generator that writes none of it. It writes the index now, and a fresh generation is compared against the checked-in copy.

- 2026-08-28 (v2.5 Phase 6 backlog 1+2): Made the 52-week window mean one thing. `market_evidence` reads a leader's distance from a 52-week high off `max(highs)` and refuses to publish one at all until a full window has completed; the eligibility gate took the same phrase off the closing series and published one from a window of any length. So the harness's own hardest gate held the looser of two readings of a phrase it used twice. The source settles neither question -- it says "at least 30 percent above its 52-week low" and "within at least 25 percent of its 52-week high" and never says which price or how much history -- and a full search of both corpora found nothing that does.

  The extremes now come off the whole bar. The two criteria move in opposite directions under that change and only one of them can lose: an intraday low is at or below the closing low, so criterion 6 can only get easier, and an intraday high is at or above the closing high, so criterion 7 can only get harder. Measured over 464 names on the 2026-08-27 session, criterion 6 gained 28 passes and flipped none out, and criterion 7 lost 8. The largest gaps are single-print wicks -- one name read 17.75% below its high on closes and 47.44% below what it actually traded at.

  The window is bounded by a date rather than a bar count -- 52 weeks, which is 364 days and not a calendar year -- and a history that does not reach back that far measures neither criterion. `min(252, len(close))` had no coverage requirement, and the error a short window makes is directional rather than noisy: a truncated window has a higher low and a lower high, so criterion 6 reads a fail a full year would have passed and criterion 7 reads a pass it would have failed. Against the full-year reading, a 200-session window -- already "sufficient" history by the existing test -- disagreed on 22 and 23 names of 464, and never once in the other direction.

  The false fail was the expensive one, because `evaluate_eligibility` reads a standard failure before the recent-IPO route opens. A stock too young to have a 52-week low was rejected on its 52-week low, ahead of the route that exists for stocks too young to have one -- unavailable evidence producing a guessed fail, and the guess becoming AVOID. A history's bar count still decides `history_state` on its own: whether a 200-day average is measurable and whether a year of extremes is measurable are two separate questions, and tying them to one threshold would refuse readings that are perfectly available.

- 2026-08-28 (v2.5 Phase 6-D/6-E): Split the prose layers. A rule stated in both the root constitution and a skill body is read twice in one window -- skills load into the context that already holds `CLAUDE.md`, and there is no isolation boundary between them -- so what the constitution owns was removed from both skills: web-search scope, failure versus missing evidence, PROCEED not being BUY-READY, current taxonomy never projected backward, market weakness not selling a ticker, visual judgment not overturning a deterministic failure, the completed-session disclosure, and the Power Play waiver. Duplication *between* the two skills stays: routing loads one or the other, never both, and each sentence is that skill's own procedure.

  The constitution gave back what the interface already owns. The `role` paragraph ran to 923 characters -- eight percent of the file -- and `doctrine.show` states the same thing in its own `limitations`; the `status` vocabulary is per-capability in `describe`; the sentences describing what `--no-cache` and `--format` do were the field descriptions in `capabilities.py`, verbatim in effect; and the three skill-routing bullets restated two skill descriptions that are themselves always loaded. What came back after cutting too far is as instructive: the envelope schema lists twelve fields as equals and never says which five carry the answer, and nothing in `doctrine._readable` refuses a quarantined claim -- it refuses `out_of_scope` only -- so that sentence in the constitution is the rule's only home and was restored.

  One live drift closed. `ticker.risk` emits `{"action": "REDUCE", "fraction": 0.5}` from a cited claim, while the constitution, both skill bodies, and both skill descriptions said never to prescribe position sizes. An analyst reading that literally swallows a reduction the harness computed. The scope now separates a within-position fraction, which is sell discipline, from an account-level weight, which stays out of scope -- and narrows further than the original decision: report the fraction a risk envelope measured and cited, never invent one.

  Seven rules deferred since the planning session landed with the space this freed: the response language rule, asking for trade traction in plain words rather than handing back `needs_input`, naming an earnings release still ahead of the session, reporting a base count against its band with the source's disclaimer, the first sessions out of the base as the earliest reading of time-as-evidence, and the six leader categories reading the same growth numbers differently.

  The budget is now bytes rather than lines, because one line of soft-wrapped prose ran to 923 characters and the line count scored it as one. The ceiling in `test_root_constitution_is_thin_principle_first_and_interface_driven` is 11,012 bytes -- the size the file had while the skills still duplicated it -- and the file is 10,995.

  An adversarial round found four reproducible items, three of them planted by this slice. The added earnings sentence asserted a rule for a prospective entry that no registered claim and no code path supports: both earnings-calendar claims are scoped to an active position, so recording an unbuilt intent as constitutional text was the very drift this slice removes, and the sentence was narrowed to the hold. The new sizing scope covered reductions and left additions open, which is where a numeric progressive exposure would have entered. The completed-bar rule said price where the decision said price and volume, and volume is not price -- a running breakout bar's volume read as settled evidence. The fourth was a rule cut from a skill that lost its only home: what an INCOMPLETE would take to resolve, which the envelope does not supply either, since `next_capabilities` is empty on an incomplete qualification. Fitting the fixes under the ceiling took one more piece of dead prose with it: "do not call legacy modules directly" describes something that cannot happen, and `test_v1_runtime_retired` enforces the absence. Collection 1843 to 1843.

- 2026-08-28 (v2.5 Phase 6-C): Gave `data` a contract. The shared envelope schema pins eleven of its twelve fields by name under `additionalProperties: false` and left the twelfth -- the only one whose shape differs per capability -- as `{}`, so an envelope carrying `{"bogus": true}` was a valid `ticker.qualify` response and so was one whose keys a refactor had renamed.

  A fixed required list cannot be written honestly: `ticker.qualify` alone emits two keys when the provider fails and a dozen when it measures. What can be written is the vocabulary -- every top-level key a capability can put under `data` -- plus the subset every domain envelope carries. `Capability` gained `data_keys` and `data_core`, `schema_sync` bakes them under `data` with `additionalProperties: false`, and any capability can also be answered by `error_envelope` under its own operation name, so the schema names both shapes as a `oneOf`. They are disjoint by construction, which is why it is `oneOf` and not `anyOf`: an envelope matching both is a domain answer that grew an `error` key, and that is neither.

  The vocabulary was measured twice and cross-checked. A read-only Codex batch traced each `data=` site into its reducer and enumerated the keys; the whole suite was then re-run with `contracts.envelope` wrapped, recording the keys of every envelope actually built. No observed key fell outside the static list. `data_core` was derived the other way, by reading the branches rather than intersecting what the suite happened to exercise -- a key that every tested branch carries is not a key that every branch carries, and freezing the first as `required` makes the schema reject a real envelope from a branch nobody ran. A top-level key the compact filter strips by name belongs to the vocabulary and never to the core.

  Two public seams disagreed about the same pair of capabilities. `capabilities` and `describe` were routed only in `cli.dispatch`, so `execute` answered them with the unimplemented-operation envelope -- under their own operation name, carrying a `reason` key no vocabulary admits. Both now live in `execute` and `dispatch` delegates.

  Nothing in this repository had ever validated its own published schemas; the existing contract test compares the schema files against the declaration they were baked from, which cannot catch a constraint written at a depth that makes it inert. `jsonschema` is now a test-only dependency in `scripts/requirements-dev.txt`, installed by `bootstrap.sh`, and `tests/schemas.py` joins the shared helpers. A sweep runs all twenty capabilities in both formats against their own schemas, from two runtimes -- every boundary refusing, and fixture evidence -- plus three cases the table cannot reach, exercising 167 of the 168 declared keys.

  An adversarial round on the finished branch got through in one place, and it was planted by the fix itself. Routing `capabilities` and `describe` into `execute` put them behind the default `Runtime(cache=ProviderCache())`, so a `MINERVINI_CACHE_DIR` the process cannot resolve turned reading the interface into an `internal_error` -- on `main` both returned normally, because `dispatch` answered them before any runtime existed. They now answer before the runtime is built rather than merely without using it: these two are what an analyst reaches for when something else is already broken. Collection 1831 to 1843.

- 2026-08-28 (v2.5 Phase 6-G): Audited the price consumers the last normalisation slice never reached. `setup_structure.read_bars` is this harness's one definition of a usable price history, and five surfaces read through it; the other four each normalised the frame themselves, and the gaps between their rules reached every verdict the harness makes. The eligibility gate was the least defended of them, which is the worst place for it: its own reading was `pd.to_numeric(errors="coerce").dropna()`, precisely the laundering that shared reading exists to stop. Twelve histories it refuses were measured here and published `ok` -- boolean, complex and timestamp closes, a session printed twice, two intraday stamps on one date, an index of positions, a close below zero, a high under its own low -- and two of them left as a raised exception where the envelope is the contract. Two were worse than a wrong number: a history whose closes were half holes was measured on the survivors, and 150 rows is under the 200 the standard route needs, so a gap in the data left through the exception reserved for a stock too young to have those sessions; a provider printing every session twice was measured over twice as many rows, so the 200-session average the source asks for spanned a hundred trading days.

  The market and peer surfaces failed the same way in opposite directions. `_ohlcv_rows` sorted nothing and stamped `completed: True` on every row, so complex leader prices lost their imaginary part and read as an ordinary advance -- regime favorable -- while boolean prices became a flat line at 1.0 sitting on its own 52-week low -- regime defensive. Neither history is a price series. In `ticker.peers` the drift ran through the session date: that surface alone converted a tz-aware index to New York where everything else drops the zone and keeps the wall clock, so a UTC-stamped history had its last session renamed to the day before, and the function refuses a history whose last session is not the analysis session. An unmeasurable peer is dropped from the ranking and the survivors are renumbered, so a leader this harness reads perfectly well improved the target's published rank by being unreadable.

  `ticker.risk` is the exception, by decision rather than by omission. Slice 260827 had deliberately given it a finer contract -- a repeated session resolved by date to its last print, a history without a Volume column still measured, an unreadable bar reporting the prefix already audited -- and refusing those histories wholesale would take the harness offline for a held position where today it answers with a named partial. So the split is between absence and wrongness: a hole is a price that is missing, and a boolean, a complex number or a timestamp is a price that is *wrong*, which `float()` turns into 1.0, a real part and epoch nanoseconds. `read_price_kinds` carries the value and index rules without the whole-window ones, and `tests/260828/unit/test_two_readers_one_vocabulary.py` writes down the five places the two readers are allowed to differ -- holes, repeated sessions, impossible bar ranges, the Volume column, and the clock time. That last one is what the file actually caught: normalising the index to midnight in both readers destroyed the information the "last print wins" rule reads, and handed the choice to row order.

  Two things were closed on judgement rather than frequency. Arithmetic that leaves the float range produces `nan`, every comparison against which is False, so an overflowed 200-day average arrives dressed as an average the price failed to exceed and a history meeting all eight criteria returns AVOID; and `inf`, which reaches the envelope that the CLI then cannot serialise at all, in a contract whose whole premise is exactly one envelope per command. Across 48 names in six industries measured live, no derived value came near either. What made the 52-week-high basis a backlog item rather than a fix is the opposite measurement: three of those 48 sit on different sides of the 25% criterion depending on whether the high is the highest close or the highest session high, and one of them -- MRVL -- is eligible today and would not be. The source says "within 25 percent of its 52-week high" and does not settle which, `ticker.qualify` and `ticker.peers` currently agree on the close, and changing it is a doctrine edit that starts by re-reading the registry's quotations.

  Two test fixtures were part of the defect rather than incidental to it. The leader fixture in `tests/260829` gave every bar an `Open` below its own `Low`, and the `peer_collection` fixture was a four-row frame carrying nothing but a close -- neither is a shape the provider sends, and the unrealism is exactly where these defects were hiding.

  An adversarial round on the finished branch got through in three places, two of them planted by the fix itself and all three the same shape: the code contradicting a rule the slice had just written down. The rule was "holes are kept" -- and `nan` passed while `None` and `pd.NA` were refused as wrong types, so which sentinel a provider happened to write decided whether an already-established stop breach came back as SELL or as INCOMPLETE. The new reader's comment claimed the corporate-action column was checked under its own rules and it was carried unchecked, so `True` coerced to 1, 1 reads as "no split", and a price halved by a share split became a breach nobody stopped out of; the check that closes it is narrower than "is not a number", because a column of words cannot coerce and the split audit already withholds itself by name, which says more than refusing the history. And reporting every unmeasurable criterion as a gap revived the recent-IPO exception as required missing evidence: that route exists for a stock with too little history to have a 200-day average, so an envelope qualified a stock and claimed required evidence was missing in the same breath. Collection 1801 to 1831.

- 2026-08-28 (v2.5 Phase 6-B): Closed the composition surface. `ticker.risk` mints the only BUY-READY this harness produces and read its four component planes -- market, eligibility, setup, fundamentals -- from words the caller typed. Four of them on a ticker that does not exist returned `ok`, `BUY-READY`, and an empty `sources` list: the envelope reported that it had read nothing and ruled anyway. The rule that closes it is one sentence, and it is the general form of the argument `tests/260825/unit/test_power_play_waiver.py` had already recorded for one word: a caller's word may only move the verdict toward no-trade. A declared failure still fails and a declared wait still waits, both conservative and both already how doctrine reaches AVOID from a known failure; a pass is what a word can no longer buy. Passing now comes from the envelope the measuring capability emitted, attached through a repeatable `--evidence FILE` and checked for being about this ticker, this session, and having measured anything at all, with the plane's word read from that envelope's own payload rather than from whatever was typed beside it. The attachment is a reference and not a flag, because `attested: true` would be the same defect one level up. A refused envelope is reported with the check it failed rather than dropped, since "your evidence did not count" with no reason attached sends a reader to re-run a capability that was fine. Active-mode market defense is unchanged -- it tightens stops and touches no verdict.

  An adversarial read of the first commit went through it in five places, and four of them were the code contradicting the sentence the commit had just written down. Introducing a reference had moved what a caller could forge rather than removing it: an attestation composed in an `execute` request, or a hand-assembled dict carrying exactly the fields this capability reads, both passed. So what is checked is the shape a capability actually emits -- the key set derived from the envelope builder itself, so a key added to that contract is one an attachment must carry without anybody remembering this check -- plus a non-empty `sources` list, because an envelope that reached no provider measured nothing and that empty list is what the original defect published beside its own BUY-READY. An `attested_by` arriving in a request is discarded unconditionally, since a request that attaches nothing is exactly where a forged one has nothing to compete with. An attached envelope was also overwriting a declared failure, which is the rule backwards and, in active mode, took a holder's own defensive market read away along with the stops it tightens; a declared fail or wait now stands over an envelope that passes, and the reference records that it did. Two envelopes claiming one plane are refused rather than ranked, because keeping the last handed the verdict to argument order. The request echo keeps the envelopes it was given so that request can be replayed, and the references moved to `data.attested_evidence` where a finding belongs. One older defect surfaced in the same read: the shared state reader preferred `setup_state` over `state` on every object, so a risk plane carrying both turned a declared failure into a pass -- the alias is now per plane. Prose changed nowhere but the capability's own contract: that is the only place this rule was ever taught, so there was no second copy to drift.

  New suite: `tests/260828` grows the unit and integration cases; `tests/attestations.py` joins `series.py` and `readings.py` as a shared test helper, and builds its envelopes through the real builder so a test fixture cannot be a shape the capabilities never produce. Collection 1779 to 1801.

- 2026-08-28 (v2.5 Phase 5): Rewrote the market and leadership plane. The defect the audit found was the same one Phase 4 had just closed on the fundamentals side, wearing market clothes: a verdict input with no evidence under it. `_leader_evidence` read a `behavior` word off the caller's own row, and the RS source that supplies those rows ranks tickers and says nothing about behaviour, so the field was a placeholder. `_group_evidence` was worse -- five of its six slots were permanently `unavailable` and the sixth reported `observed` off the presence of a rating. A placeholder reports `observed`, `observed` is not `supports`, and `leader_traction` was one of the four signals `favorable` required. The favorable regime was therefore unreachable from any live snapshot, not because the market never qualified but because one of its inputs could never be met.

  A leader is now read from its own completed bars, against three claims the corpus states plainly: how far it sits from a new 52-week high, whether it prints on the 52-week-low list, and how deep its correction from the last peak has run. The band never carries the state alone -- the correction's own gate is the only reading here that can refuse, and the low list is a filter the source stated outright ("stay away from this list and all of its components"), so it contradicts on its own. The depth is measured forward from the peak rather than across the window, because a decline that happened before the peak is not that peak's correction and reading the window's low against its high reports a recovery as a drawdown.

  A group is harder, and the honest answer is smaller than the plan had assumed. The RS source publishes a group average and a member count and no membership at all, so the only names the harness can place inside a group are the market-wide ranked leaders it already fetched. Every group reading is a sample of the market's leaders and never a census of the group, and the sample is published beside every count. What that sample can answer is `market.group_new_highs_signal`, which the registry records as deterministic and which the source stated as a direction rather than a threshold -- a growing number of names making new 52-week highs signals a group advance is under way. The source named no window to measure the growth over, so the length is registered as a harness parameter: the low end of the 4-to-8-week window the same corpus gives for a new bull market's leaders to reveal themselves, converted to sessions through the trading-week convention. Group membership comes from a mutable current classification, so a snapshot taken against a past session reads no membership at all and says so, rather than attaching today's taxonomy to a historical answer.

  Then the regime rule itself. `favorable` required `market_breadth == supports`, and the breadth block's best state is `observed`: Finviz publishes only a live page, the harness already labelled it context-only in its own output, and the registry holds no breadth threshold at all -- re-mining found the corpus never names one. The defensive rule's breadth arm was dead for the same reason. What can carry a verdict here is what the harness measures against doctrine: the ranked leaders read from their own bars, and the trader's own realized traction. The index switch is a real measurement that refuses a favorable call when it has gone off and does not produce one when it is on, which is what a practice-layer switch labelled context-only should do. Completeness moved to `evidence_quality`, where it belongs -- a group gap used to turn the whole regime `incomplete`, which is the envelope's `status` question answered in the verdict's voice.

  `market.candidates` stamped `recommendation_state: not_recommended` on every row it paged and a `recommendation_count` of zero on every page, because nothing on that path reads a price. A constant published as a judgment reads as one -- a name the harness weighed and declined -- when no name there has been weighed at all. The reducer supported an upstream judgment that had no producer anywhere; the field is gone and the prose points at `ticker.qualify`, which is where a name earns a word. The snapshot's `doctrine_ids` stopped being the literal `["scope.data_integrity"]` and is derived from what the payload names, with the two conventions that sized the growth window cited beside the count they produced.

  Two adversarial review rounds by an independent model followed, and both reversed things the phase had just decided. The first round's three real findings were the same defect three ways: a reading that claimed more than its evidence held. `leader_traction` dropped every unread leader before aggregating, so one measured name beside nineteen unread ones published `supports`; the QQQ 21-EMA switch had been promoted to a favorable-regime veto, which the claim's own registry note forbids in as many words ("Do not promote this to a hard gate"); and the group-advance signal, which the source states for "names in a particular industry", was being cited for sectors. The paragraph above describing the switch as a measurement that refuses a favorable call is what that round overturned: the switch now carries nothing, and `favorable` rests on the two signals this harness measures against doctrine.

  The second round found that two of the first round's own corrections had stopped one step short, and one had overshot. The leader majority had become two nested majorities -- most of what answered, out of most of the list -- which is not the majority the source asked for: four names holding ground beside four never read is four of ten, and the count is now taken against the ranked list. The correction depth's running peak read each session against its own high, and a daily bar records a high and a low and never which printed first; measuring a session against the peak the sessions before it established costs a real intraday crash some depth and refuses to invent an ordering no completed bar supports. This reversed a test the first round had written the other way. The overshoot was the sector correction: a claim scoped to industries was being filed as evidence a sector withheld, so the summary now says `not_applicable` where the claim does not reach, and the missing list names which group reading went unread rather than speaking only for the new-high count. Three more findings closed the gap between what the snapshot published and what it read: `leader_limit` bounded the fan-out but not the published leader population, so unread names sat in the reducer's denominator; a frame whose `iterrows` was not callable still reached the loop; and a frame of three hundred rows carrying no price escaped the missing list, because "the frame was empty" was only one of the ways a history can arrive unreadable.

  A regression-only round then ran against the second round's own fixes, per the termination rule the fundamentals phase had settled, and found that one of them had planted a regression of its own. Avoiding the high/low ambiguity, the corrected correction depth had discarded the one intrabar order a completed bar does record -- the open prints before both -- so a bar that opened at a new high and sold off the same session reported the confirmed decline from that open as if it never happened: an open of 100 falling to a low of 40 read as the 43% measured from the prior peak, not the 60% the open states, and a single leader could reach a false favorable off the difference. The low is now measured against the higher of the prior sessions' peak and this bar's own open, never this bar's high, and a bar carrying no open falls back cleanly to the prior peak. It is the same shape as the two the second round closed: a fix narrowing a helper's window dropped information the helper had been reading. Verification: 1777 deterministic tests, 264 of 270 quotations verified against the extraction with six declared departures.

- 2026-08-28 (v2.5 Phase 4): Rewrote the fundamentals plane. The audit that opened the phase found something a layer deeper than the plan had recorded: `fundamentals.py` had never imported `doctrine` at all, so every number in it was invented and the envelope cited one claim -- `scope.data_integrity` -- while the registry held thirty-three mined `fundamentals.*` claims wired to nothing. The permanent INCOMPLETE had a mechanical cause in the same place: the evaluator read `going_concern` and `accounting_integrity` off the filing mapping, and the SEC provider has never emitted either field, so the live path could not reach a verdict on any ticker. A corpus search settled the worst of the invented numbers -- the ten-percent dilution threshold matched zero occurrences of `dilut` in either book.

  What replaced them is the role contract, applied claim by claim. Every `fundamentals.*` claim carries `failure: needs_review`, so none of them produces a verdict alone, and the growth thresholds are bands rather than gates: "many successful growth managers require a minimum of 20 to 25 percent" is a range the source gave as a range. Each band now reports the measurement, the range, and which edge is the good one. The one gate among them is Code 33 -- three consecutive quarters where earnings growth, sales growth and the net margin all came in above the quarter before, ending at the latest filed quarter, because "a Code 33 situation" is a condition a stock is in now rather than the longest run anywhere in its history. The source named the count and declined to name a magnitude, so any improvement counts and no minimum step was invented.

  Two of those bands turned out to name a window as well as a range -- "in the most recent one, two, or three quarters", "in the most recent two to three quarters" -- and only the range had been read. The windows are registered as references, which are never compared with a measurement, so they decide how many quarters get reported and nothing else; the headline stays the latest quarter, the one every length of the source's window contains. Before that, a stock whose latest quarter came in soft behind two strong ones was published as though the two strong ones had never been filed.

  The same discipline decided what does not get computed. Three claims live in filing footnotes this harness does not read -- stripping a nonrecurring gain out of reported EPS, the habitual one-time charge, the tax-disclosure gap -- and each is published as a standing boundary rather than omitted, because an omitted reading and an unrunnable one look identical to a reader and only one of them is a boundary. The categories defined by a peer group, and the peer halves of the low-multiple warning and of return on equity, report what they needed instead of a reading computed from the one company this evaluator holds. Four figures the corpus records are population or illustrative statistics rather than standards -- that ninety percent of the biggest winners showed acceleration, that their multiples ran 30 to 40 times, that a growth stock can trade at three to four times the market -- and none of them is compared with a ticker's measurement anywhere.

  Category is now what it was always for. The same earnings history means different things depending on what the company is, so only the declared category's claims run: a turnaround is held to the hundred-percent bar and the two-strong-quarters gate, where "strong" means what the registry's own cross-reference says it means, and "near or above its old peak" publishes both figures and names the word nobody quantified rather than inventing a tolerance. A market leader is read against a marker at twenty percent and a band at 35-45 over the best five-to-ten-year stretch on file. The practice layer travels beside all of it, quoted rather than summarised, because a judgment-only claim's whole content is a sentence somebody said and the difference between "I never look at P/E" and "I rarely concern myself with the P/E ratio" is exactly the point.

  The price-earnings family needed a price, which the filings do not carry. Asking the caller for one would let a number that disagrees with the completed bars decide a reading, so `ticker.fundamentals` reads the last completed session itself and derives the breakout-session close from the same history. The multiple at the breakout is computed from what had been filed by then; today's filings would credit the buyer with a quarter published six weeks after they bought and shrink every expansion the claim exists to notice. A price provider that cannot answer leaves the valuation readings unavailable and does not stop the filings from reaching a verdict, because the price is not filed evidence.

  A calendar provider closed the last gap. Two claims already read a held position's next earnings date and could only fire when a caller happened to type one; an unstated date is a gap in what the analyst typed, not in what is knowable. The snapshot follows the same discipline as the classification snapshot and refuses a historical `as_of` outright -- a calendar entry is a forecast, and dating today's answer to a past session puts a schedule nobody published then inside a point-in-time verdict. Two dates from the feed is a window it guessed at; the earlier edge is published, because a holder asking whether a report is still ahead needs the first session it could land on, and the block says whether the date was declared or fetched and whether anybody confirmed it. The planned entry-side proximity gate was not built: the corpus registers no claim for it, both earnings claims declare `active_position` context, and reading a claim outside its declared context is the same invention this phase exists to remove.

  Three defects surfaced only under live data, after the deterministic suite was green. The annual balance-sheet bucket identified a fiscal year by asking whether the date ended in `-12-31`, which silently dropped every balance a September or January filer ever filed; the year a balance belongs to is the one whose income statement closes on the same date, which the same taxonomy dump carries. The period merge replaced a whole period when a later filing mentioned it, and a quarterly report carries last fiscal year's balance sheet with no income statement for that year -- so the latest year came out holding equity and nothing to measure it against, while every earlier year, whose quarterly reports had aged out of the eligible window, looked complete. Later-filed wins per field, which is what a restatement is. And a compound per-share rate spanning a stock split reports a company that shrank: the filed EPS for a pre-split year is restated onto a share base four times larger in every later vintage. The split is a price-history fact and this capability holds filings, so nothing is adjusted -- instead every compound per-share rate travels with the total-earnings rate a split cannot touch and with what the filed diluted share count did over the same years.

  Adversarial review ran on fixed lenses -- doctrine, arithmetic, provider boundary, envelope consistency -- because a new lens each round makes "no defects" unreachable by construction. Twenty-one findings; the ones that mattered were all the same two shapes the earlier phases produced. A published field contradicting what sits beside it: the quarterly growth band alone was producing `supports_convergence`, so a company whose annual earnings and sales had both halved cleared it on one quarter landing inside 20-25 percent -- and convergence is the source's own word for a conjunction, so no single band may carry it. And one question answered in two places that had drifted: the same breakout date returned a gap from one test's contract and a refusal from another's.

  Provenance moved from the period to the field. A 20-F filer carrying US-GAAP revenue and IFRS equity in one period had the whole period stamped with one basis, and the margin built from it divided one regime's earnings by another's sales; each number now carries its own `accounting_basis` and `filed_at`, and a measurement spanning two regimes refuses to compute rather than publishing a ratio of them. Elapsed months became completed months, because a window the source gave in months counts months that have finished and a month that has not reached its own day has not.

  Then live data found what no fixture had. Every 10-Q carries two duration facts for the same closing date -- the quarter's three months and the year-to-date run-up to it -- and the provider read both as quarterly, so a nine-month cumulative figure was published as one quarter's earnings and measured against the growth bands. The period label had the same disease as the balance sheets did: a fact with no `frame` fell back to `fy`/`fp`, which name the fiscal year of the report the fact was printed in, so one quarter arrived as `2026-Q2` from the report that first published it and as `2027-Q2` from the next year's comparative column, and the two never met. Both are now settled by the fact itself: length decides whether it is a quarter or a year, and the calendar quarter its own span sits in decides its name -- one convention for a January close and a September close alike.

  That repair exposed the larger one. No US filer publishes a fourth-quarter column, because the 10-K states the year in its place, so "a trailing year is four consecutive filed quarters" was a definition no ticker could satisfy -- NVDA, ANET and AAPL all came back naming four consecutive filed quarters as what the price-earnings ratio lacked. The trailing year is now the last closed fiscal year plus the quarters filed since it minus the quarters those replace: three filed numbers, no reconstructed quarter published into the series where Code 33 and the growth bands would measure it. The route the number came by travels with it. The fiscal year is found by its closing date rather than its label, because a January or September close makes the label name a year the quarters do not.

  A mutation sweep over the phase's arithmetic and provider boundary found four rules with reasons written beside them that no test defended: the quarter before the first is last year's, the count of quarters Code 33 needs comes from the registry, ninety percent of a turnaround's old peak is not its old peak, and a best stretch cannot span a year nobody filed. Each has a test now, and each was confirmed to fail with the mutation applied.
  A second review round, on the same four lenses, produced twenty-four findings; eighteen reproduced and were fixed, two were rejected with the reasoning recorded, and four were duplicates of repairs already made. The period name was the first. Naming a period from the midpoint of its own span let a restatement move it, because `start` is the end an amendment edits -- a 10-K/A correcting a 52-week year to 53 weeks pushed the midpoint across the new year, so the correction arrived as the prior year and was compared against the original it was correcting. A period is now named from its close alone. A projection can still collide, so two facts with different closing dates that reach the same name are no longer merged: the period is withheld whole and its name published in `missing`, because quietly keeping one of them is the shape this phase has fixed over and over.

  Field-level provenance had been read in one place only. The margin asked whether its two numbers were measured under the same accounting basis; the year-over-year pair, the annual pair, the trailing year and return on equity all read whatever the period carried, so a US-GAAP profit was divided by IFRS equity and an IFRS revenue was measured against a US-GAAP one for growth. That question now has a single home and refuses the computation when the answer is no. The provider hole underneath it closed with it -- the balance-sheet concepts were searched for under US-GAAP names while the `ifrs-full` taxonomy was already being accepted, so a 20-F filer's equity and inventory disappeared before provenance could ever see them.

  Missing evidence was being published as negative evidence in three more places. A latest quarter filed without EPS dropped out of the series, which promoted the quarter before it to "most recent" and published a stale growth rate as the company's current one; the headline is now the latest filed quarter or nothing. A turnaround whose second branch could not be measured was published as `false` rather than unknown. And sequential acceleration answered zero, while the history lookback answered `false`, when no comparable pair existed at all.

  Two findings were rejected. Code 33's absence is not a conflicting finding -- the source names it as a strength pattern, and the registry's own `failure.meaning` says a failure is a conflicting finding -- so requiring it would let only triple-accelerating stocks support convergence, which is not the minimum the source stated. And rounding before the gate is deliberate: `evaluate_band` compares the value it publishes, so that a decline computed as 19.999999999999996 and printed as 20.0 does not also report itself as below twenty. Letting the gate compare raw while the band compares published would manufacture exactly the contradiction this phase keeps removing, at a precision far below what a filed EPS carries.

  A second sweep, widened to eighty-five mutants over the guards those rounds added, killed seventy-three and left twelve. Eleven were rules with reasons written beside them and nothing asserting them: a decade at one growth rate ties with itself at every span the source's range covers and the tie now goes to the fewest years the rate held; return on equity read the latest year with both figures rather than the earliest; the accelerating run stops at the four quarters the source looked over and a repeated growth rate does not extend it; earnings outrunning sales needs both from one quarter; the one-to-two-year lookback reads to its far edge; one 10-Q beside a 20-F means the registrant files quarters; a fact and its submission have to name the same form; a session past `as_of` is not the last completed close; and a breakout date past `as_of` is refused as that, not as a session the bars happen not to carry. The twelfth is an equivalent mutant -- the trailing year's window bounds are subsumed by the completeness check running beside them, identical over four hundred randomly generated filing sets -- and the reasoning is recorded rather than pinned by a test that could only restate the code.

  A third round, on the same four lenses, produced twenty-one findings and every one of them reproduced. The provider was where the worst of it lived. The accounting taxonomy was chosen once for the whole company and US-GAAP was checked first, so the day an IFRS filer's first 10-K appeared, every year that filer had ever published under IFRS stopped existing -- for an `as_of` before that 10-K as well, which is a future document deciding a past answer. Both taxonomies are read now and each fact carries the regime that measured it. The unit went the same way: SEC stores a concept once per unit array, and collapsing them made a hundred US dollars beside a hundred and thirty Canadian ones into thirty percent of growth. And the record deduplication keyed on the accession and the projected period name alone, so of two facts with different closing dates the first one sent survived -- which meant the collision rule the previous round added never saw the second close, and the answer depended on provider order. Three narrower gaps closed with them: the IFRS diluted earnings concept was missing from the alias list, form 40-F was dropped entirely although this harness covers ADRs, and a price index entry that is not a date coerced to `NaT` and silently removed a complete session from a history whose coverage still claimed it was whole.

  The previous round's repair of "the latest quarter" had reached one band and stopped. The superperformance band, the practitioner band, the sequential run and the source's own three-quarter window all still read the tail of a series that drops the quarters whose figure was absent, so a quarter filed without earnings promoted the quarter before it to current. The trailing twelve months did the same thing under a live price: a year closing in December divided into a May close, published as `reported` with a route beside it. One helper is now the only place that asks whether a series still reaches the latest filed quarter. Beside it, three arithmetic guards: a quotient can overflow when its inputs and sums do not, and the state was being chosen before the value it describes existed; `inf >= inf` was answering the turnaround's peak question from two numbers the reading refuses to publish; and deceleration and the quarterly share change took the last two surviving points of a series, which over a hole in the filings is a rate from two years ago called "previous".

  The doctrine lens found three places where a source's words had been read as something narrower than they are. "Historical study of superperformance stocks shows that the average P/E increased... on average (or two to three times)" is a population statistic and therefore a reference, never a range this ticker sits inside; the conditional half of the same passage, which is about this stock, stays a band. "During their best 5- or 10-year stretch" names two lengths and none between them, and reading it as a range let a six-year window win the rate published under the source's own 35-to-45 band. And "twice or more without explanation" attaches a condition to the finding that lives in prose this harness does not read, so the ratios are published and the source's phrase for them is not -- the unread half named the way this evaluator's three footnote claims already name theirs. The same shape settled the turnaround: nobody quantified how near is near, so a trailing year below its old peak has not failed "near or above", and that criterion can be satisfied or open and never refused.

  Last, the envelope. `status` describes contract completeness rather than verdict polarity, and a negative verdict a declaration settled was reading as `ok` with four required items sitting in `missing` beside it. It is `partial` whenever anything is missing. `declared_inputs` had omitted the market regime the payload reads the bull-market band with, so the envelope disagreed with its own reading about what the caller supplied.

  The rounds were then stopped by rule rather than by a clean result. Three rounds produced twenty-one, twenty-four and twenty-one findings, none of them repeats, and the tail of each round reached inputs no filer can produce. On a surface this size "a round with no defects" is not a reachable condition, so the standard is now: close every confirmed finding, run one regression-only round bounded to those fixes, fix what reproduces from a plausible filing, and merge. New defect classes go to the next phase's backlog.

  That regression round found seven, and every one was the same shape: a rule closed at one call site while the same question was still being asked the old way somewhere else. The unit reached provenance and stopped there, so the year-over-year comparison and the twelve-month sum still saw only the regime -- and a filer that changed reporting currency published an exchange rate as a year of growth. The record deduplication key gained the closing date but not the regime, so a transition filer tagging one period under both taxonomies lost the second before the grouping built to hold it could see it. The impossible-bar refusal ran before the `as_of` filter, letting one bad row past the boundary refuse a history that was complete through the session requested. The latest-filed anchor reached the bands and not Code 33, the margin trend, the smoothed pair, the deceleration flag or the cyclical direction. The collision rule removed a period before the anchor was derived from it, so the envelope named the collision while a trailing year a quarter out of date sat beside it as `reported`. And the double-trouble condition, which the source states about both balances at once, was two gates each applying the combined limit to one balance alone. All seven are closed and the round ends there by the rule above.

- 2026-08-27 (v2.5 Phase 3): Rewrote position management so a held position has a vocabulary between "keep holding" and "sell". The old contract answered one question -- did price reach the stop -- and everything else a trader does while holding had nowhere to go, so a position with an untouched stop and a broken structure came back as a bare HOLD. Three verdicts stay (HOLD, SELL, INCOMPLETE) and `management_actions` carries the rest: RAISE_STOP, REDUCE, REVIEW. Only a breached stop, a breached invalidation, or an average the trader declared as their own exit plan can sell. Twenty-six mined rules arrived as six vertical slices, each closing its own claims, reducer, measurements and tests together, and each reviewed adversarially before the next began.

  Where a rule is measured from turned out to matter more than how. Three R is measured from the highest completed High since entry rather than the last close, because a position that reached three R and gave it back is precisely the one the rule protects. An initial stop is audited over the window it actually governed -- entry through the eve of a raise -- after a review showed a raised stop hiding a breach the earlier level would have caught, and a stop that was later widened keeps governing, because stops are not widened. A declared management average (`--management-average ema21|sma50`) sells on two closes below it, not because TraderLion's rule binds this harness but because the trader declared that average as their exit plan and the harness audits declared plans without negotiation; the same two closes below the average that was not declared are a REVIEW. TraderLion's own stage defaults reach the action vocabulary only through `--management-profile`, stamped non-binding, and never reach the verdict vocabulary at all.

  Structure is measured and mostly reported rather than acted on: two closes below the trailing average with the quality of those closes, a close under the 20-day, the largest decline of the whole advance in both timeframes, the extension over a declared base top against TraderLion's 20-25 pause zone, extension from each average in percent and ATR beside the stock's own history of extensions, three of six key-reversal criteria with the three that a chart or the source's own silence leaves open reported unresolved, gaps since the breakout, a climax vector, and whether a down session since the breakout traded heavier than the breakout itself. Two of those become REVIEW actions and none can sell. Time is evidence in its own right -- sessions since entry, sessions since the last new closing high, every natural reaction with its depth and how long recovery took, and Zanger's first two sessions out of the base reported as contrast. A deteriorating tape raises the stop toward the 5-6 percent loss range the source cuts at in a difficult market and never sells the ticker on an opinion of the market; the sizing half of that same passage is deliberately unimplemented, because this harness never prescribes size.

  Six adversarial review rounds rejected the work and produced twenty-six production defects, every one reproduced before it was fixed. Nearly all were one shape: a published field contradicting what sat beside it. A number rounded across its own gate, so `3.0` stood next to "three R not reached". Two readers of the same frame deduplicating a repeated session differently, so one said SELL and the other said clear -- and after the first fix, deduplicating by timestamp rather than by session, so two prints of one day at different clock times both survived. An audit that returned at the breach and then reported the whole window as checked. A window naming a date the provider printed no bar for. `doctrine_ids` citing claims the payload never used, and later citing a claim id a caller had invented. A tie for the largest decline dated at the earlier occurrence, then fixed only for the final session. NaN reaching `evaluate_gate` and coming back as `fail`, a verdict word for an arithmetic accident. And the fix for one of those was itself the same shape at a larger scale: voiding every measurement in the frame because one ancient bar was unreadable erased evidence that was fine, so each block now asks about its own lookback and no other.

  Two more rounds found the last of it, and both findings were the harness answering a question nobody asked. Post-breakout doctrine -- the tennis-ball bounce, the first two sessions out of the base, the gaps and the key reversal since the breakout -- fell back to the entry session when no breakout was declared, which reads what a stock does after it breaks out into an early or cheat entry that has not broken out yet. Those blocks now withhold under `breakout_date_not_declared`, and so does the pause-zone review. And the split guard, which refuses a window whose declared level and closes sit in two coordinate systems, was silently passing any history that carried no split column at all -- so an unadjusted two-for-one read as a fifty percent fall and sold the position. Absence of the column is absence of evidence, the same reading the Power Play measurements already took, so the closes are now asked for the discontinuity directly at the ratio of the smallest ordinary split. The excursion asks the same question the stop audit asks, from the same helper, because it was reading peaks across events the audit refused.

  Five further rounds, each running two reviewers with different lenses, produced fifty more. The shape held: a published field contradicting what sat beside it. Among the levels one price or one session broke, the record was naming the lower one, though price falls from above and the highest is the line it crossed first. Two exits both happened and the later one named the failure, so a stop print weeks after a declared average had already closed the position claimed the record. A split-sized discontinuity refused a whole window from its first session, erasing a breach the sessions before it had honestly found -- an event two days later cannot un-take-out a stop the market took out, so an audit now runs up to the uncrossable session and refuses only from there. A widened stop was measuring the loss while the path beside it still called the older level live. And `tighten_to_is_placeable: true` sat next to `current_price: null`, because not knowing had been read as yes.

  The last round reached the two rules the rest had been circling. The first is that a level is a level of some price: a hard stop is an order resting in the market and the tape takes it out intraday, while a structural invalidation is a statement about where a session finished -- the condition a caller writes beside one says "completed close below" -- so auditing an invalidation on the Low sold a position on a poke that closed back above it, and put a condition in the trader's mouth. The two are now audited against different prices and each record names its basis. The second is that a price handed in carries a date, and that date is `as_of`, the latest one any exit can have. Treating it as terminal skipped the completed-bar audit entirely, so an invalidation the bars broke three weeks earlier lost the record to today's stop. Assertions have no date and still settle the verdict without the bars; a price does not.

  The same round narrowed the block guards one further step, from the window a reading spans to the cells it opens: a twenty-session return opens two closes rather than twenty-one, and eight blocks had been refusing over bars they never touched. Its mirror is that a statistic over a population opens every member, so a hole among the fifty sessions a volume ratio averages produces no ratio rather than an average over the sessions that happened to be there -- and a split inside a volume population is that same hole reached from the other side, since the volumes before it count a different share. `claim_inputs_not_read` stopped being a list kept by hand beside each block and became the subtraction it always described: the block names what it opened, and what its cited claims asked for beyond that is arithmetic. Both hand-kept values came back unchanged from the derivation, which is the evidence that the wiring is right.

  A further round, reading the reducer's action surface and the published prose against the code that has to honour it, found nine more and settled the rules the two before it had only half-stated. An assertion settles the verdict, not the record: it says the position ended without saying when, so a live-stop breach asserted today was claiming a trade that a close had already invalidated a week earlier. The bars are read now even for a settled verdict -- what the settlement buys is that their absence cannot downgrade it, which is what an earlier round had really been protecting when it stopped fetching them at all. A stop is a price the position transacts at and an invalidation is a threshold the thesis has to be carried through, so one includes its level and the other does not, and a close that stopped exactly on the invalidation had not gone below it. Inside one session the ordering is not the levels' but the prices': the Low prints before the close, so a session that took out a resting stop intraday and invalidated at the close ended at the stop, and the earlier rule about the highest level applies only among levels read from the same price.

  The rest were the same two rules landing in the places they had not yet reached. A window refused because it spans a corporate action is refused for the price handed in too, and that price is withheld with it -- otherwise a fifty-percent split reads as a fifty-percent fall and sells a position the market never took out. Whichever observation the record was built from is the one published beside it, because one session cannot have two latest prices. The highest High since entry is a statistic over every session held, so a hole in it withholds the peak rather than publishing the highest readable High under a name that promises the highest -- three R is measured from that number, and three R raises a stop. And an audit that stopped at an unusable Low reports the sessions it had already cleared, the way the split refusal already did. One action was renamed to what its evidence actually established: a down session heavier than the breakout is that comparison, not the source's low-volume-breakout-then-high-volume-selling pattern, whose two halves the corpus never bounds.

  Two more rounds went after what no earlier round had: the arithmetic itself, and the verdict as a whole rather than any one module. The arithmetic round found that a comparison average was demanding one bar more than it reads, that a tie was decided on the raw binary figure rather than the one published -- so two declines that both print as the same percentage were dated at the earlier of them because their last bits differed -- and that a figure rounded for publication was being fed back in as a divisor, making an extension of 7.35 true ranges come out as 7.3500000007. A measurement now returns two things: the record to publish, and the number to keep computing with.

  The verdict round found six, and they resolved into two rules the branch had been stating in one place and not the other. The first is that every reader of a level reads it the same way: a price handed in was still testing an invalidation with the stop's own predicate, so a close exactly on the level counted as below it, and a close below both levels was attributed to the invalidation when a close that low proves the resting stop had already filled intraday. The second is what an assertion is worth. It outranks evidence nobody gathered; it does not outrank evidence that was gathered about the same levels and says the opposite, which is a request contradicting itself and comes back INCOMPLETE naming that rather than as either half of it. And a state word on its own is not the audit it is shaped like -- with no session and no level, and sometimes naming a role for a level the request never declared, it is an assertion, and it meets the bars like one. The last was a boundary the two readers of one frame disagreed about: a split is stamped on the session that printed the new prices, so a position opened on that session was opened in the new coordinates and never crosses anything. The excursion already read it that way; the stop audit did not.

  A fifteenth round ran two lenses at once -- a reader who had never seen the branch, and one supplying deliberately hostile input -- and its seventeen findings split cleanly into the same two shapes the branch has been finding all along, one aimed at the code and one at the caller. On the code side: how far a position got was being read from two different windows, so the extension block credited the entry session's own High while the stop rules, which cannot tell whether that High printed before or after the fill, did not. A cleared audit of closes was clearing a stop that rests under the Lows, which it proves nothing about -- the implication runs one way only, and now says so. A declared-average exit that had already triggered was being erased by an unreadable close three weeks later, when the values before a hole are computed from readable closes and a finding among them already happened. And an unreadable cell in the split column was being filled in with a zero, which turns missing evidence into an assertion that nothing happened -- the one move a gap may never make, and the same one an earlier round had already caught in the column's absence.

  The hostile lens found that the request itself had never been read as strictly as the bars. An unrecognised state word was silently a "no", a list where an object belonged was silently absent, a date written as a number parsed in one half of the harness and read as missing in the other, and a `current_price` of zero sold a position while an infinite one was quietly discarded in favour of the provider's -- the same field failing in opposite directions. Those are now refused at the request seam, against the same vocabularies the flags already enforce. Two findings were about arithmetic that had escaped the arithmetic round: a stop declared above the entry it protects leaves the trade no risk to measure R or reward-to-risk against, and a price small enough to round away at the reported precision turns every quotient built from it into an infinity printed beside a zero. The last was the branch's own recurring shape in the data rather than the code: a session whose close sits under its own low is not a session, and the audit reading Lows called that window clear while the price read from the Close sold the position. Refusing the bar whole found two fixtures inside this repository that had been describing impossible sessions.

  A sixteenth round put two more lenses on it -- the arithmetic itself, and whether the published page tells the truth about itself -- and found eight. Two were regressions from the round before, and both are the same mistake: moving a shared helper's boundary changes what every caller's argument means. Narrowing the excursion window had carried the split guard along with it, so an event the session after entry fell outside a window it is squarely inside; and making `split_position` skip its own first session inverted a `-1` written to compensate for the behavior it used to have. The contract lens found that the prose written in the round before was ahead of the code: a handed-in record was still being accepted without the basis and the crossing price the bars' own record always carries, and its date was checked against the position rather than against the window the level it names was actually in force over. One owner now answers that question for both the audit and the record.

  The rest were the branch's usual shape at a smaller scale: a cap printed at four decimal places beside a stop checked at ten, so the page showed a failure the verdict had not reached; a gap count citing the closing-range formula in a history with no gaps to compute one from; a climax publishing a null range with nothing named as missing; and an excursion that fell silent on the day a position was opened while the rule beside it read the last close as the floor. The mutation sweep contributed two findings of its own kind: a clause guarding blank event cells and a clause catching a high beneath its own low were both provably doing nothing, each subsumed by the test on the line under it, and were deleted rather than kept as decoration.

  The review stopped there rather than at a clean verdict, and the reason is recorded as a decision. Giving each round a new lens makes "no defects found" a statement about the lens rather than about the branch, so it is not an arrival. By the sixteenth round the findings had moved from ones that flip a verdict to ones that affect how a number is printed, and two of the three arithmetic findings were cleanup after the previous round's fixes. What remains of that kind is the declared scope of a later phase.

  One further finding was rejected. A reviewer asked for exchange-holiday validation of provider bars. This harness carries no trading calendar, says so in its own published limitations, and imitating one from business days would replace a stated gap with a second untruth.

  One finding was rejected rather than fixed. A reviewer read `{"action": "REDUCE", "fraction": 0.5}` as prescribing position size. What this harness refuses to prescribe is how much to buy -- portfolio weight, account allocation, size -- and how much of a position already held to sell is a selling discipline the source states as a rule. It is reachable only through an explicit `--management-profile`, and the capability's own first limitation already draws that line.

  Three findings were recorded rather than fixed, with the reasoning in the plan and the limitation in the capability's own prose. A stop that moved twice cannot be expressed in this contract, and the intermediate level is therefore unaudited. A session missing in the middle of an audit window cannot be told from a market holiday without a trading calendar, which this harness does not carry; what it publishes instead is the first and last bar that actually spoke. And an INCOMPLETE verdict drops its measurements while a SELL keeps them, against a reviewer's request for symmetry, because they are different states: INCOMPLETE means the request never established the position those windows are keyed to.

- 2026-08-25 (v2.5 Phase 2-2): Rewrote the setup engine so the verdict rests on measurements the bars can contradict, and so READY has to be earned rather than defaulted into. The old contract took `--price-geometry pass` and `--supply-evidence pass`, assertions with nothing behind them, and produced `ready` whenever nothing was marked failed or missing -- which is how twenty-two drifting bars and two flags reached the top verdict. The caller now declares coordinates instead: an alternating chain of swing dates ending on the high that is the pivot. Every claim in that chain is falsifiable, and a bar named as a swing high that is not the highest bar in the span its neighbours bound comes back as a contradiction naming the date. Structure resolution, measurement, and judgment became three modules with three owners: the resolver decides whether a chart reading survives the bars, the measurement module knows no doctrine at all and takes its window lengths as an argument, and only the reducer reads the registry. Each entry route declares the evidence it must positively have, by claim id, so a setup cannot back into readiness by having no objections. Contrast evidence is separated before either half reaches the reducer, because `contrast_pass` is a word no reducer's state vocabulary contains and handing one in would not be ignored -- it would read as missing evidence and turn another practitioner's disagreement into this harness's incompleteness. Thirteen new registry claims carry the VCP measurements, including two the source states as filters with no number in them at all: volume must be bigger on up days than on down days, and every correct pivot develops with a contraction in volume.

  Ten adversarial review rounds by an independent model rejected the work and produced fifty-three further defects, every one reproduced before it was fixed. Most were about where a number was read rather than how it was computed: the breakout was taken from the last bar of the history, so a pivot two months old could be paired with today's volume; the depth limit was handed the depth of whatever the caller called the base; the pivot volume gate applied a comparison to a value the registry had marked undecidable, bypassing the marker role added one slice earlier; and `pause_held_to_breakout` checked the base's low while its name promised the pause's. Three were regressions introduced while fixing the round before, and each is recorded as one. Two are worth keeping as rules rather than as fixes. The first is that the source's modal verb is the role: "must", "every", and "insist" are gates while "should", "we want to see", and "quiets down noticeably" report, and the split runs inside a single sentence -- binding "a few of the price spikes to the upside should be large" to the same gate as "the volume must be much bigger on up days" let a clause the source hedged reject a candidate on a hair's difference. The second is that a measurement is only as good as the moment it is read at, which is what all four of the largest findings had in common.

  The sharpest finding was not a number. The engine could not verify that a declared chain describes the base's whole structure -- a chain that skips an unfavourable contraction has anchors that are genuinely their spans' extremes, so no measurement separates it from an honest one -- and it issued READY anyway. Declaring a gap and stepping over it is worse than not knowing about it, because the verdict then looks earned. Three readings now stand where measurement cannot reach: whether the right side is V-shaped, whether the chain is complete, and whether the entry is close to the pivot or chased. Two of them the bars can refuse, and the third cannot be self-certified at all: the reading exists to check the caller's chain, and the chain's own author cannot perform that check, so `complete` counts only from an independent segmentation. Until the swing detector supplies one, the standard route stops at wait -- the same constraint the plan already placed on a detector working alone, arriving from the other direction. All three readings are listed back in the envelope so the share of the verdict that came from a person is visible.

  Two of the readings turned out to be flags again, and the fix for one of them was itself a flag. Requiring an independent segmentation to vouch for a caller's chain closed the self-certification hole, and then the words "independent segmentation" were published as a command-line option -- a supplier's name typed by the supplier is not provenance. That option is gone and a request naming it is refused, which means no request reaches READY on the standard route until the swing detector exists to produce the comparison. The entry-proximity rule cut in the wrong place twice: comparing the entry with the breakout's own extension called a twenty-percent gap that ticked down "at the pivot" and refused a one-cent advance the day after a three-percent breakout, because it never looked at the absolute distance. Every mechanical rule tried there either invented the number the source withheld or drew a line somewhere the source did not, so the call is the reader's outright and the distances are printed for them, with Minervini's own five-to-twenty-cent buffer reported as the band it is. A third finding was that the source separates two failures and the code counted one: a close under the base's own low needs a whole new base, and no later rally makes the declared structure the one being bought, while a slip below the pivot can reset and recover.

  Later rounds kept finding the same shape in new places: a rule meant to close a hole that closed it with a word. Requiring an independent segmentation and then publishing "independent segmentation" as a flag anyone could type. Accepting the other segmentation and then allowing a finer caller chain "between the same endpoints", which lets an unfavourable twenty-eight percent contraction be re-cut into four small ones without skipping an anchor or moving an end -- so a segmentation now vouches only for itself, exactly. Refusing an entry the source calls chased by using the one distance the source did quantify, five to twenty cents, which made a band decide a required condition its own record says it may never decide, and put the boundary somewhere absurd: a dollar below the pivot read as inside the range. Then treating a declared entry price as available because it fell inside the latest bar's low-to-high range, which is a fact about a session that has closed and does not survive the observation, written in the same capability, that completed bars cannot prove a price is still on offer. What the verdict reads now is the latest completed close, because that is a price the tape recorded.

  Where the trust boundary sits is stated rather than papered over. Chain completeness is checked by comparing two segmentations and cannot be talked past. How close is close, whether a right side is V-shaped, and whether a pivot failure reset promptly are the reader's calls with the measurements printed beside them; the harness does not invent a limit the source withheld in order to stop a reader from declaring against a number in front of them. A test fixes each of those as accepted rather than overlooked, so changing one means changing a test that says why it was accepted.

  One reading of the source was simply backwards and is recorded as such. The correction window had been narrowed to the leg the base sits in, on the reasoning that a decline the stock had fully recovered from belonged to the previous base because the rally back had worked off its overhead supply. The passage says the opposite in its own next clause: a stock that fell more than half "could fail as it reaches or slightly surpasses a new high. This is due to excessive overhead supply created by the steep price decline." The recovery is when the danger arrives. Verification: 528 deterministic tests, 207 quotations verified and 6 declared, `validate_harness.py` clean, ten adversarial review rounds by an independent model, which produced twenty-two blockers and thirty-one lesser findings; six of those were regressions introduced while fixing the round before, and each is recorded as one.

- 2026-08-25 (v2.5 Phase 2-1): Gave the threshold registry a fourth role and separated what kind of statement a number is from whose standard it belongs to. Three roles could not carry two cases the corpus actually contains. "Each successive contraction contained to about half (plus or minus a reasonable amount)" names a value and then explicitly refuses to bound it, which is not a gate, not a range, and not a population statistic, yet it is compared with a measured ratio; that is the new `marker`, which reports the measurement, the value the source named, and the distance between them, in a state word (`reported`) deliberately outside the vocabulary the reducers branch on. Ryan's "the volume should increase at least 25%" is an unmistakable filter that Phase 1 recorded as a population statistic for a reason that had nothing to do with what kind of statement it is: it must not decide anything here. Those two questions now have two answers. A claim outside canonical Minervini doctrine may hold a real gate, and `evaluate_gate` stamps it non-binding and reports `contrast_pass` or `contrast_fail`, so a reducer scanning states generically cannot hand another practitioner's standard a verdict. The prohibition moved from the registry, where it distorted the record, to the reducers' own manifest. Re-auditing every threshold against the sentence it cites moved thirty-three of the seventy-seven references: filters demoted for the wrong reason, one range the source gave once and the registry had split in two, and values compared against a ticker while labelled population statistics. Six claims were promoted to holding gates while their own prose still said "this is descriptive or statistical context, not an executable pass/fail rule", and `doctrine show` printed both; `validate()` now refuses that contradiction. An adversarial review rejected the first version and found seven more, all fixed: the binding guarantee was fail-open, because attribution was optional and a missing name read as the house voice, so deleting one line from Ryan's claim made his standard bind with the registry green -- the canonical layer must now name its voice, and silence is an incomplete record; `threshold()` handed back raw values regardless of role or binding, so a manual comparison bypassed the contrast stamp entirely, and markers and non-binding values no longer leave through that seam at all; eight of the thirteen new markers cite quotations that draw the boundary in words ("more than", "at least", "greater than", "twice or more") and are gates; two promotions to `band` contradicted the claims' own notes, which said in as many words that the figure was open-ended and not a measurement, because the audit read the quotations without reading the notes beside them; three gates added for Power Play and stage-2 confirmation had no consumer, no manifest entry, and no pass/fail behaviour, and were removed to arrive with the slices that wire them; the test presented as proof of the marker role synthesized its threshold in memory and attached it to an unrelated quotation, so it tested nothing about the registry; and the validator did not stop a contrast gate from declaring a rejection effect. A second round found six more, all fixed: `threshold()` still handed back a band's pair, whose meaning is where a measurement sits and so belongs to `evaluate_band` for the same reason a marker's does; the harness-layer exemption from the no-rejection rule was inferred from the layer rather than named, so any harness record could have claimed it, and it is now one listed claim whose rejection is about the request contract; three claims held a gate while saying missing evidence did not apply to them, which reads a filter with no measurement as irrelevant rather than unanswered; thirteen claims' provenance still said the threshold was "never promoted to a gate", and `doctrine show` prints provenance, so the contradiction was one the reader saw; required attribution was satisfied by an empty string; and a promoted gate read a session count its claim never listed as an input. Four findings were recorded rather than fixed here, because they belong to the slices that wire the affected code: the non-binding state words must be split into their own channel before reaching reducers that would otherwise degrade them to missing evidence, a marker's signed distance is deliberately exposed because the response standard asks for it, claim-level attribution cannot express a threshold whose operationalization is the harness's own, and two promoted gates encode one branch of a composite condition and must not be evaluated alone. Verification: 398 deterministic tests, 190 quotations verified and 6 declared, `validate_harness.py` clean, two adversarial review rounds by an independent model.

- 2026-08-24 (v2.5 Phase 1): Rebuilt the doctrine registry from 12 claims to 124, each carrying the source text it rests on. Five corpora were mined, including one 65,000-character multi-practitioner Q&A that earlier passes had never opened; its four practitioners disagree on the record and are recorded as four separate claims rather than one house view. A threshold now declares its `role`, because the corpus is mostly hedged prose and compiling it into pass/fail manufactured boundaries the author never drew: a `gate` is a limit the source states as a filter and admits no proximity argument, a `band` is a range the source gave as a range and reports where the measurement landed, and a `reference` is a population statistic never evaluated against a ticker. Eighteen gates exist, all inside the twelve core claims; not one mined claim was promoted to a filter. `technical.py` and `risk.py` now read every limit and its required-value text from the registry, proven by tests that move a threshold and watch the verdict follow. Deleted `quarantine.ch12_failure_cascade`, which had no supporting passage in either corpus. Position-sizing doctrine is recorded with `out_of_scope` and pinned away from every capability, so the prohibition is structural rather than remembered. Added `scripts/verify_doctrine_quotations.py`: `validate()` can see that a quotation is well formed, and this sees whether the words are the author's. It found ten of 157 quotations were not what they claimed; all now verify or declare what they bridged. An adversarial review then rejected it and found eight more, all fixed: `assembled_from` was an unconditional bypass that counted a fabricated sentence as verified, so a declaration now explains non-adjacency and never excuses a fragment absent from the source; the verifier erased decimal points, so a quoted "7.5 percent" altered to "75 percent" read as the author's own number; a band and a duration reference were deciding Primary Base eligibility, contradicting the contract, so depth is decided by gates alone and the band travels beside them; gates compared rounded measurements, reintroducing the proximity tolerance the role exists to forbid; `validate()` accepted a gate whose value was a boolean and a registry missing a threshold a reducer reads by name, both of which produce a false verdict or a mid-verdict crash with the registry green; harness-layer and non-Minervini-attributed claims could hold hard gates; and `out_of_scope` was metadata rather than a barrier, so `threshold()` now refuses to hand back a number the harness may not act on. A second review round found ten more, all fixed: removing the five-week boundary had let a four-week base claim the year-long fifty-percent allowance, so that boundary is back as a gate governing the exception's availability rather than the depth ceiling, which is the same either side of it; the depth band was computed and then discarded before the envelope, making the disclosure rule prose nothing carried out; the required-threshold manifest protected a name but not the role a reducer reads it as, so a gate swapped to a band validated and then handed a list to a comparison; a harness-layer claim could still hold an executable gate; the verifier erased range dashes and ratio colons, so "30-40" and "3040" normalised alike; its fragment check dropped anything under twenty-five characters, so a short fabricated sentence appended to a declared quotation went unexamined, and coverage is now walked across the whole string; NaN passed the numeric check and turned an ordinary stop into a rejection with the registry green; one band's direction was inverted; and the commit had swept in a machine-specific history symlink. A third round found six more, all fixed: the verifier preserved range dashes but still erased comparison operators and minus signs, so a cited filter of `> 69` altered to `< 69` verified as authentic; its coverage walk rejected a genuine assembly whose last passage was shorter than the run floor, so it now asks whether the remainder exists rather than rejecting on length alone; a two-figure contrast the source draws between a prior growth rate and a fallen one had been modelled as a directional band, where a lower reading would report as better than the passage's own "material deterioration", and is now a pair of references nothing evaluates; band measurements exposed raw binary-float noise where gates already rounded what they report; and one new test asserted the opposite of its own name, passing while reporting exactly the band it claimed was absent. A fourth round found that a citation made only of punctuation cleared both validators, because each counted raw characters where forty periods look like a sentence; both now count letters and digits. It also found the band audit unfinished, and a sweep of all forty prompted by it demoted fourteen more: a band is a range a measurement lands inside, and a historical average of declines that already happened, a count of names to track, and a window chosen rather than measured are none of those. A fifth round found one scan window the sweep had missed, and accepted the remaining twenty-five bands as real measurements. Roles settled at eighteen gates, twenty-five bands, seventy-seven references. Verification: 366 deterministic tests, 190 quotations verified and 6 declared, five adversarial review rounds by an independent model.

- 2026-08-17: Rebuilt the harness as v2 around principle over rail, interface over document, dense progressive disclosure, typed point-in-time providers, composable deterministic reducers, explicit research state, two shared host skills, and detailed just-in-time CLI help. Retired v1 agents, rules, workflow, reference libraries, trade-review route, duplicate Codex skill link, and legacy runtime substrate after preserving the v1 baseline for release.
- 2026-08-18: Repaired the provider layer after an AAOI session found three providers dead and the runtime reporting `ready: true`. The RS outage recorded in the v2 live report as an external provider outage was a local CA bundle the python.org interpreter never populated. Moved bar-completion to the provider boundary, withheld domain verdicts built on evidence that stops before the requested session, bumped the cache schema so entries holding an unfinished bar can never be served, preserved and redacted boundary failure causes, replaced the Finviz calendar-day gate with a session-open gate and demoted its breadth to context evidence, added `health --probe` plus offline configuration checks, and added a session-start readiness hook. Verification: 208 deterministic tests, two adversarial reviews by an independent model.
- 2026-08-17: Closed the adversarial behavioral blockers by auditing the full active stop path and treating hypothetical evidence as a closed world; bounded candidate exclusion evidence after live smoke exposed a roughly 200,000-token response; completed 167 deterministic and artifact tests plus a 30-report independent behavioral gate.
- 2026-08-24 (v2.5 Phase 0): Fixed four verified judgment-plane defects found by a five-source audit. An active invalidation level is now compared with the completed price path and the latest price instead of being read as a status flag, and the audited protective level is the higher of the hard stop and the invalidation because a long position reaches it first. A missing `average_gain_pct` degrades to explicit missing evidence rather than silently skipping the half-average-gain cap. Primary Base depth follows the source's three-week, three-to-five-week, and longer-correction bands, with 35-50% sent to chart review and anything past 50% rejected; emergence became a separate trigger signal so an unbroken all-time high is unfinished timing, not AVOID. The chart renderer is imported lazily so discovery, help, and every deterministic capability run on a machine without the plotting stack. Corrected the Codex skill link to its real path `.codex/skills`. New suite: `tests/260824`; discovery root moved to `tests/`.

  An independent adversarial review of that diff rejected it and found six further defects, all fixed in the same change: a stop raised later was auditing the older invalidation level only from the newer date, so each protective level is now audited from its own effective date and the path carries per-level `audits`; an invalidation that is only condition text, or whose price is not a price, no longer supports HOLD and is reported as `auditable_protective_level`; a clear path must now name the level it audited and the window it covered instead of inheriting the caller's stop; the source's second emergence route (a constructive consolidation near the all-time high) and its year-long 50% correction band were declared in doctrine but unreachable, and each now has an explicit weekly-chart input; and the offline test's own probe demanded matplotlib be installed, which would have failed on the very machine the fix is for.

  A second review round rejected those fixes and found three more: an audit could still be counted for a level whose window it started after, or whose own state was unresolved, so coverage is now checked per level against the date that level took effect; a structural invalidation given only as condition text no longer permits HOLD beside a cleared stop, because HOLD asserts nothing has invalidated the thesis and an unevaluated condition is not part of that assertion, while a known breach now outranks missing evidence instead of being erased by it; and non-finite numbers passed straight through to a JSON encoder that cannot carry them, so prices are validated at the argument boundary where the wrong value stops being representable.

  A third round found the last two: an audit window was never checked against the decision date, so a path that stopped days earlier still produced HOLD — the reducer now takes `as_of` and requires each audit to span its level's effective date through that session; and the breach short-circuit erased every missing field, so a position entered after the decision date, or one with no exit plan at all, could be sold. Missing evidence is now split from request coherence: a known breach outranks evidence nobody gathered, never a request that contradicts itself.

  A fourth round found the split had overcorrected: an absent entry price was blocking an unambiguous stop breach, though entry economics decide 3R protection and never whether a level was crossed; an invalidation mapping carrying only a status was counted as a declared exit plan, so a bare `triggered` flag could sell against nothing; and a breach that already settled the verdict still fetched daily history, letting a provider failure downgrade a terminal SELL to `partial`.

  A fifth round showed why those kept recurring: the operation was restating, in its own words, two questions the reducer already answers — is this breach settled, and was an exit plan declared — and the two copies drifted apart in both directions. `risk.settled_breach` and `risk.declares_exit_plan` are now the single owners and the operation asks them, so routing cannot disagree with the verdict it routes to. A sixth round found the same shape one level down — two ways of reading a state word, one trimming and reading `status`, one not — so `_status_word` is now the module's only reader. Verification: 286 deterministic tests, six adversarial review rounds by an independent model.
