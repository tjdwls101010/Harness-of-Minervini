This sandbox has no network, so any provider-backed command will fail after its retries. That is a real environment gap and you report it as one -- it is never a reason to substitute a number, and never a reason to assert something the envelope does not establish. Where the user has stipulated facts, they are the evidence you reason from; check what the registry actually says about each one with `pipeline doctrine show <claim-id>` rather than assuming a stated threshold is a gate. A `gate` decides pass or fail; a `band` and a `marker` report where a measurement sits and can never carry a verdict alone.
