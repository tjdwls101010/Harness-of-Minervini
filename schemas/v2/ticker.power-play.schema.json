{
  "$id": "https://harness.minervini.dev/schemas/v2/ticker.power-play.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    },
    {
      "properties": {
        "operation": {
          "const": "ticker.power-play"
        }
      },
      "required": [
        "operation"
      ]
    }
  ],
  "title": "Harness of Minervini v2 ticker.power-play response",
  "x-capability-contract": {
    "errors": [
      "invalid_request",
      "provider_unavailable",
      "internal_error"
    ],
    "examples": [
      "scripts/.venv/bin/python scripts/pipeline ticker power-play AAOI",
      "scripts/.venv/bin/python scripts/pipeline ticker power-play NVDA --as-of 2026-06-19"
    ],
    "exit_codes": {
      "0": "A valid domain envelope was emitted; its status may still be partial, unavailable, or needs_input.",
      "2": "The request or arguments were invalid; stdout contains a needs_input error envelope.",
      "3": "An unexpected internal contract failure occurred; stdout contains an unavailable error envelope."
    },
    "inputs": {
      "as_of": {
        "description": "Completed US trading session in YYYY-MM-DD form; defaults to the latest completed regular session.",
        "required": false,
        "type": "date"
      },
      "format": {
        "choices": [
          "compact",
          "full"
        ],
        "default": "full",
        "description": "Output detail only; compact preserves verdict, signals, and missing evidence while omitting verbose basis rows.",
        "required": false,
        "type": "enum"
      },
      "no_cache": {
        "default": false,
        "description": "Bypass both provider-cache reads and writes for this invocation.",
        "required": false,
        "type": "boolean"
      },
      "ticker": {
        "description": "US-listed ticker symbol.",
        "required": true,
        "type": "string"
      }
    },
    "limitations": [
      "This is an evidence pack, not a qualifier. Two of the four criteria end in questions completed bars do not answer -- whether the launch volume was huge rather than merely expanded, and whether a flag outside ten percent nonetheless shows VCP characteristics -- so a structure that clears every measurable limit comes back incomplete naming the reading it waits on. What it does deterministically is remove candidates: an advance short of a hundred percent, a flag past six weeks or deeper than twenty-five, and an advance with no expanded session anywhere in it are all findings the bars support on their own.",
      "Nothing here waives verified fundamentals. The exception the source describes is real and this is what it would have to be measured against, but the surface that could close it needs each control's own evidence rather than a caller's word for it, and no capability holds all of them yet. ticker.fundamentals keeps its growth gap as a gap.",
      "The structure is found rather than declared, inside two windows anchored at the last completed bar: the peak is the highest session of the longest structure the criteria describe, and the advance is the eight weeks before it. Anchoring both at the last bar is what keeps the reading still when a caller loads a different amount of history -- measured against the whole history's maximum instead, fourteen of eighteen real tickers reported a different peak, a different advance, or both. The cost is that a structure whose peak sits further back than the criteria allow is reported as the failure it is rather than searched for.",
      "The eight-week and six-week limits are enforced by not looking past them, which is not the same as forgiving what lies beyond: an advance that took nine weeks is never found by a search that looks back eight, and reports whatever it managed inside eight instead.",
      "The advance is gated on the close-to-close reading and the low-to-high reading is reported beside it. Read on the extremes alone, a single intraday wick to half the peak reports a hundred and four percent advance on a stock whose close moved half a percent. Flag depth runs the other way -- the intraday reading is the deeper and stricter one -- and is measured on the extremes for that reason.",
      "Which session an advance began on is not something the bars settle, so the volume clause is read across the advance rather than off one bar: the heaviest session in it is reported with its date. The lowest session is not reliably the first one -- a quiet undercut five weeks before the peak wins the lowest-low search, and reading the clause there reported no expansion at all on a stock that doubled in nine sessions at ten times its usual volume.",
      "A marginal new high inside a tight flag restarts the structure, because nothing in the source says how large a new high has to be before it means something. A flag making a new high every few sessions never accumulates the twelve the source requires and comes back incomplete; a flag that stops making them can have an earlier long flag re-read as advance. Both are peak identity questions rather than flag age ones, and neither is resolved here.",
      "Corporate actions are named, not corrected. The provider supplies the split events and the prices stay the ones the tape printed, so a split inside the measured span -- which runs back through the volume baseline, not just the structure -- is reported and the structure is left unresolved. A history that does not carry the event column has not said there was no split, and that absence is reported as its own gap: without it a one-for-two reverse split is indistinguishable from the hundred percent overnight advance these criteria ask about.",
      "The fingerprint covers the five price columns and not the corporate actions, so two inputs with identical bars and different split events digest the same. That is why no chart approval is accepted here yet: an approval bound to that digest would not be bound to the evidence the verdict actually turns on."
    ],
    "output": "The advance, the flag and the volume behind them, each of the source's four criteria read against what was measured, and whether the structure is out, unresolved, or clear on everything the bars can settle.",
    "prerequisites": [],
    "side_effects": [],
    "status_meanings": {
      "ok": "The bars settled it: at least one criterion the source states failed on measurement.",
      "partial": "Nothing measurable failed, and the structure is not qualified either -- a chart reading, corporate-action evidence, or both are still missing, and each gap names its own cause.",
      "unavailable": "Completed price history is unavailable, or the history is unusable as bars at all -- the reason names which, in the same vocabulary ticker.swings and ticker.chart use."
    },
    "summary": "Measure the Power Play criteria against completed bars, so the one exception to verified fundamentals has to be earned rather than declared."
  }
}
